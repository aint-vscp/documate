---
phase: 02-wire-gaps-found-in-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/api/validate-document/route.ts
  - src/types/index.ts
  - src/components/document/ValidationTierBadge.tsx
  - src/components/document/index.ts
  - src/app/dashboard/documents/new/page.tsx
  - src/app/dashboard/documents/[id]/page.tsx
  - src/app/dashboard/profile/page.tsx
autonomous: true
requirements:
  - DOC-03
must_haves:
  truths:
    - Gold/Silver/Bronze validation tier is visible in document trust flows.
    - Validation attestation and confidence are available to UI consumers through typed contract.
    - Validation tier naming is distinct from subscription/account tier naming.
  artifacts:
    - path: src/app/api/validate-document/route.ts
      provides: typed producer payload for validation tier and attestation metadata
    - path: src/components/document/ValidationTierBadge.tsx
      provides: shared trust-tier badge rendering component
    - path: src/app/dashboard/documents/new/page.tsx
      provides: tier visibility in pre-sign validation flow
  key_links:
    - from: src/app/api/validate-document/route.ts
      to: src/types/index.ts
      via: shared validation result interface
      pattern: validationTier(Name|Attestation|Confidence)
    - from: src/components/document/ValidationTierBadge.tsx
      to: src/app/dashboard/documents/new/page.tsx
      via: component import and render of API tier output
      pattern: ValidationTierBadge
---

<objective>
Wire TEE trust-tier output from API producer to user-visible dashboard surfaces using a shared typed contract.

Purpose: Make DOC-03 provable in the live user flow, not API-only.
Output: Tier metadata contract plus reusable badge rendering integrated in document and profile trust views.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-wire-gaps-found-in-audit/02-RESEARCH.md
@src/app/api/validate-document/route.ts
@src/app/dashboard/documents/new/page.tsx
@src/app/dashboard/documents/[id]/page.tsx
</context>

<scope_boundary>
Implement only DOC-03 trust-tier visibility wiring. Do not redesign unrelated dashboard layouts or add new verification providers.
</scope_boundary>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and expose typed validation-tier contract</name>
  <read_first>
    - src/app/api/validate-document/route.ts
    - src/types/index.ts
  </read_first>
  <files>src/app/api/validate-document/route.ts, src/types/index.ts</files>
  <behavior>
    - Test 1: Validation API response includes explicit tier code/name/attestation/confidence fields.
    - Test 2: TypeScript compilation enforces the new shape in UI consumers.
  </behavior>
  <action>
    Define shared validation result interfaces with exact keys `validationTier`, `validationTierName`, `validationAttestation`, and `validationConfidence`. Update validate-document response mapping to emit these keys for every success response. Keep backward compatibility by preserving existing fields and adding the four fixed keys.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - API producer returns deterministic trust-tier fields.
    - Shared types prevent ambiguous use of subscription tier naming.
  </acceptance_criteria>
  <done>Producer/contract surface is ready for consistent UI rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Build reusable validation tier badge component and export it</name>
  <read_first>
    - src/components/document/index.ts
    - src/components/document/TemplateGallery.tsx
  </read_first>
  <files>src/components/document/ValidationTierBadge.tsx, src/components/document/index.ts</files>
  <action>
    Create a shared tier badge UI component for Gold/Silver/Bronze states with attestation and confidence display support, and export it from the document component barrel for reuse across dashboard pages.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Single reusable component renders all supported validation tiers.
    - Component import path is stable from document component index exports.
  </acceptance_criteria>
  <done>Tier semantics are centralized and reusable for all trust-facing pages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render tier and attestation in key document and profile user flows</name>
  <read_first>
    - src/app/dashboard/documents/new/page.tsx
    - src/app/dashboard/documents/[id]/page.tsx
    - src/app/dashboard/profile/page.tsx
  </read_first>
  <files>src/app/dashboard/documents/new/page.tsx, src/app/dashboard/documents/[id]/page.tsx, src/app/dashboard/profile/page.tsx</files>
  <behavior>
    - Test 1: Newly validated document flow visibly shows tier badge and attestation.
    - Test 2: Document detail/profile trust views can surface stored tier metadata without naming collisions.
  </behavior>
  <action>
    Integrate the shared `ValidationTierBadge` component and the fixed typed fields `validationTier`, `validationTierName`, `validationAttestation`, and `validationConfidence` in existing trust display areas. Keep existing layout structure intact and inject only the tier evidence UI blocks required to close DOC-03.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }; npm run lint; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Gold/Silver/Bronze tier is clearly visible at points where users assess trust.
    - Trust-tier UI uses validationTier naming and does not reuse subscription tier labels.
  </acceptance_criteria>
  <done>DOC-03 is met with clear producer-to-consumer trust-tier visibility.</done>
</task>

</tasks>

<verification>
1. $ErrorActionPreference = 'Stop'; $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }
2. New-Item -ItemType Directory -Force ".\.planning\fixtures\phase-02" | Out-Null; Copy-Item ".\README.md" ".\.planning\fixtures\phase-02\validate-document-fixture.md" -Force
3. npm run lint
4. npm run build
5. Invoke-RestMethod -Method Post -Uri "http://localhost:3000/api/validate-document" -Form @{ file = Get-Item ".\.planning\fixtures\phase-02\validate-document-fixture.md" } | ConvertTo-Json -Depth 6
6. rg "validationTier|validationTierName|validationAttestation|validationConfidence|ValidationTierBadge" src/app/dashboard src/components/document src/app/api/validate-document/route.ts -n
</verification>

<success_criteria>
- DOC-03 trust-tier output is visible and typed end-to-end in in-scope UI flows.
- Shared tier semantics are reusable and non-ambiguous.
- Scope boundary respected: only planned trust-tier UX wiring is delivered.
</success_criteria>

<output>
After completion, create .planning/phases/02-wire-gaps-found-in-audit/02-02-SUMMARY.md
</output>
