---
phase: 02-wire-gaps-found-in-audit
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/lib/reputation/tagging.ts
  - src/lib/reputation/index.ts
  - src/app/api/activity/route.ts
  - src/app/api/directory/tags/route.ts
  - src/app/api/directory/profile/route.ts
  - src/app/api/admin/breaches/route.ts
  - src/app/dashboard/profile/page.tsx
  - src/app/dashboard/people/page.tsx
autonomous: true
requirements:
  - REP-01
  - REP-02
must_haves:
  truths:
    - Reputation tags in live UX come from evidence-backed aggregation, not local-only derivation.
    - Breach and slash outcomes are visible as persistent negative reputation evidence.
    - Each displayed tag includes provenance source and reference data (tx hash or breach record).
  artifacts:
    - path: src/lib/reputation/tagging.ts
      provides: evidence-first tag derivation rules with provenance model
    - path: src/app/api/directory/profile/route.ts
      provides: profile payload containing tags with source and references
    - path: src/app/dashboard/profile/page.tsx
      provides: evidence panel that renders tag provenance
  key_links:
    - from: src/app/api/admin/breaches/route.ts
      to: src/lib/reputation/tagging.ts
      via: breach/slash output mapped into reputation tag evidence
      pattern: BREACH|SLASH|evidence
    - from: src/app/api/directory/profile/route.ts
      to: src/app/dashboard/profile/page.tsx
      via: API payload consumed by profile trust panel
      pattern: tags.*source
---

<objective>
Connect reputation rendering to contract-history evidence and breach/slash outcomes with explicit provenance in user-facing panels.

Purpose: Close REP-01 and REP-02 with auditable evidence links in live UX.
Output: Evidence-backed reputation aggregation APIs and profile/people panel rendering aligned to chain and breach history.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-wire-gaps-found-in-audit/02-RESEARCH.md
@src/lib/reputation/tagging.ts
@src/app/api/directory/profile/route.ts
@src/app/api/admin/breaches/route.ts
</context>

<scope_boundary>
Implement only reputation-evidence wiring for REP-01 and REP-02. Do not introduce new scoring models, social features, or unrelated profile redesign.
</scope_boundary>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor reputation derivation to evidence-first aggregation contract</name>
  <read_first>
    - src/lib/reputation/tagging.ts
    - src/lib/reputation/index.ts
    - src/app/api/activity/route.ts
  </read_first>
  <files>src/lib/reputation/tagging.ts, src/lib/reputation/index.ts, src/app/api/activity/route.ts</files>
  <behavior>
    - Test 1: Tag derivation prefers chain and breach evidence inputs before local-derived heuristics.
    - Test 2: Returned tags include provenance fields usable by API and UI.
  </behavior>
  <action>
    Define reputation aggregation types to require provenance keys `source`, `txHash`, `breachId`, `referenceId`, and `evidenceTimestamp` on evidence-bearing tags. Keep helper functions only when they consume these fixed keys. Switch default derivation paths to evidence-backed inputs.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Core tagging module exports evidence-bearing tag output.
    - Activity-derived and breach-derived references can be consumed without client-side guessing.
  </acceptance_criteria>
  <done>Reputation core logic is aligned to evidence-first derivation required by REP-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire API routes to emit evidence-backed tags and breach/slash impact</name>
  <read_first>
    - src/app/api/directory/tags/route.ts
    - src/app/api/directory/profile/route.ts
    - src/app/api/admin/breaches/route.ts
  </read_first>
  <files>src/app/api/directory/tags/route.ts, src/app/api/directory/profile/route.ts, src/app/api/admin/breaches/route.ts</files>
  <behavior>
    - Test 1: Directory tags/profile endpoints return tags with provenance and references.
    - Test 2: Breach confirmation writes/returns negative evidence that feeds reputation APIs.
  </behavior>
  <action>
    Update directory and profile route payload assembly to consume evidence-first reputation module output with keys `source`, `txHash`, `breachId`, `referenceId`, and `evidenceTimestamp`. Ensure breach/slash outcomes are persisted and surfaced with explicit status and provenance references so REP-02 remains permanently visible in profile trust state.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Tags and profile APIs expose source and reference metadata for each trust label.
    - Breach/slash outcomes appear as durable negative reputation evidence in API payloads.
  </acceptance_criteria>
  <done>Server-side reputation APIs are evidence-backed and breach-aware.</done>
</task>

<task type="auto">
  <name>Task 3: Render provenance evidence panel in profile and people trust views</name>
  <read_first>
    - src/app/dashboard/profile/page.tsx
    - src/app/dashboard/people/page.tsx
    - src/app/api/directory/profile/route.ts
  </read_first>
  <files>src/app/dashboard/profile/page.tsx, src/app/dashboard/people/page.tsx</files>
  <action>
    Replace local-only trust tag rendering with API-provided evidence-backed tags and add provenance display blocks with `source`, `txHash`, `breachId`, `referenceId`, and `evidenceTimestamp` in existing trust sections. Keep current page structure and add only in-scope reputation evidence UI.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }; npm run lint; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Profile and people pages display evidence-backed tags with provenance.
    - Legacy local-only tags are not treated as authoritative trust evidence.
  </acceptance_criteria>
  <done>Live UX reputation view is tied to contract history and breach/slash proof for REP-01 and REP-02.</done>
</task>

</tasks>

<verification>
1. $ErrorActionPreference = 'Stop'; $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }
2. $walletAddress = "0x1111111111111111111111111111111111111111"
3. npm run lint
4. npm run build
5. Invoke-RestMethod "http://localhost:3000/api/activity?walletAddress=$walletAddress" | ConvertTo-Json -Depth 8
6. Invoke-RestMethod "http://localhost:3000/api/directory/profile?walletAddress=$walletAddress" | ConvertTo-Json -Depth 8
7. Invoke-RestMethod "http://localhost:3000/api/directory/tags?walletAddress=$walletAddress" | ConvertTo-Json -Depth 8
</verification>

<success_criteria>
- REP-01 and REP-02 are closed with evidence-backed reputation payloads and UI provenance rendering.
- Breach/slash signals persist and are visible as durable trust impact.
- Scope boundary respected: only planned reputation-evidence wiring changes are implemented.
</success_criteria>

<output>
After completion, create .planning/phases/02-wire-gaps-found-in-audit/02-03-SUMMARY.md
</output>
