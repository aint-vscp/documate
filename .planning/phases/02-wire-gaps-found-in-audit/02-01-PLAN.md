---
phase: 02-wire-gaps-found-in-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/documents/[id]/page.tsx
  - src/hooks/useDocuMateContract.ts
  - src/app/api/documents/route.ts
  - src/app/api/documents/[id]/route.ts
  - src/app/api/activity/route.ts
  - src/types/index.ts
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - Sender-sign cannot finalize with fallback offchain markers.
    - Finalized and pending-receiver-sign records expose strict 0x-prefixed 64-hex tx hashes.
    - Activity API only reports document anchor events from valid chain tx hashes.
  artifacts:
    - path: src/app/dashboard/documents/[id]/page.tsx
      provides: fail-closed sender-sign anchoring flow and user remediation errors
    - path: src/app/api/documents/[id]/route.ts
      provides: payload validation for transactionHash and anchor status fields
    - path: src/types/index.ts
      provides: explicit anchor status and anchor error typing
  key_links:
    - from: src/app/dashboard/documents/[id]/page.tsx
      to: src/hooks/useDocuMateContract.ts
      via: uploadDocument call and tx hash extraction
      pattern: uploadDocument\(.*\).*transactionHash
    - from: src/app/api/documents/[id]/route.ts
      to: src/app/api/activity/route.ts
      via: strict hash regex gate for anchor event projection
      pattern: \^0x\[a-fA-F0-9\]\{64\}\$
---

<objective>
Close the document-anchoring integrity gap by enforcing a fail-closed on-chain path and strict transaction reference semantics.

Purpose: Ensure DOC-01 is evidence-backed and cannot pass through fallback marker values.
Output: Updated document signing and API persistence path with strict tx hash validation and anchor metadata.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-wire-gaps-found-in-audit/02-RESEARCH.md
@.planning/phases/01-audit-partially-built-systems/01-01-SUMMARY.md
@docs/audit/01-01-document-anchor-evidence.md
</context>

<scope_boundary>
Implement only DOC-01 wiring fixes described in this plan. Do not add new product features, new chains, or unrelated schema redesign.
</scope_boundary>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define strict anchor metadata contract and hash validator usage</name>
  <read_first>
    - src/types/index.ts
    - src/app/api/documents/[id]/route.ts
    - src/app/api/activity/route.ts
  </read_first>
  <files>src/types/index.ts, src/app/api/documents/[id]/route.ts, src/app/api/activity/route.ts</files>
  <behavior>
    - Test 1: Non-hex or offchain marker values are rejected from transactionHash fields.
    - Test 2: Valid 0x64 hash values remain accepted and projected as document anchor activity.
  </behavior>
  <action>
    Add explicit anchor fields `anchorStatus` and `anchorError` to shared document types and API payload typing. Enforce `transactionHash` with strict regex `^0x[a-fA-F0-9]{64}$` in document update handlers. Preserve activity route filtering so only values matching `^0x[a-fA-F0-9]{64}$` are treated as anchor evidence. Keep compatibility for legacy records by allowing missing `anchorStatus` and `anchorError`.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Document update route cannot persist offchain markers in transactionHash.
    - Activity route classification still emits DOCUMENT_ANCHOR only for strict tx hashes.
  </acceptance_criteria>
  <done>Type and API contracts enforce strict chain-reference semantics for anchoring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make sender-sign anchoring fail-closed and actionable in document UI flow</name>
  <read_first>
    - src/app/dashboard/documents/[id]/page.tsx
    - src/hooks/useDocuMateContract.ts
    - src/config/DocuMateABI.ts
  </read_first>
  <files>src/app/dashboard/documents/[id]/page.tsx, src/hooks/useDocuMateContract.ts</files>
  <behavior>
    - Test 1: If uploadDocument cannot produce a chain tx hash, sender-sign does not advance to receiver-sign state.
    - Test 2: User gets explicit remediation guidance when anchoring is unavailable.
  </behavior>
  <action>
    Remove fallback write path that injects offchain markers. Prevent sender-sign state transition when chain anchoring fails. Keep existing `uploadDocument` integration and propagate `anchorStatus` and `anchorError` to UI state and API payload fields defined in Task 1.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Sender-sign cannot complete with synthetic fallback transactionHash values.
    - Error messaging in document detail flow tells user how to remediate chain anchoring unavailability.
  </acceptance_criteria>
  <done>UI trust boundary is fail-closed for anchoring and no longer conflates fallback with chain proof.</done>
</task>

<task type="auto">
  <name>Task 3: Validate end-to-end anchor evidence through API payload checks</name>
  <read_first>
    - src/app/api/activity/route.ts
    - src/app/api/documents/route.ts
    - docs/audit/01-01-document-anchor-evidence.md
  </read_first>
  <files>src/app/api/documents/route.ts, src/app/api/activity/route.ts</files>
  <action>
    Ensure list/read payload handling does not reinterpret fallback markers as anchor proof. Keep response shape compatible and surface `anchorStatus` and `anchorError` alongside `transactionHash`. Preserve strict tx-hash activity filtering with regex `^0x[a-fA-F0-9]{64}$`.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }; npm run lint; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Activity endpoint evidence for document anchor events is chain-hash-only.
    - Document payloads expose explicit anchor status fields without relying on overloaded transactionHash semantics.
  </acceptance_criteria>
  <done>Document APIs and activity APIs agree on anchor-proof truth model required for DOC-01.</done>
</task>

</tasks>

<verification>
1. $ErrorActionPreference = 'Stop'; $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }
2. npm run lint
3. npm run build
4. rg "offchain-anchor:" src/app/dashboard/documents/[id]/page.tsx src/app/api -n
5. $walletAddress = "0x1111111111111111111111111111111111111111"; Invoke-RestMethod "http://localhost:3000/api/activity?walletAddress=$walletAddress" | ConvertTo-Json -Depth 6
</verification>

<success_criteria>
- DOC-01 behavior is fail-closed and evidence-backed.
- No code path in scope writes offchain markers as transaction hash proof.
- Scope boundary respected: only anchoring integrity wiring changes are implemented.
</success_criteria>

<output>
After completion, create .planning/phases/02-wire-gaps-found-in-audit/02-01-SUMMARY.md
</output>
