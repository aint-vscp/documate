---
phase: 06-codebase-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - scripts/
  - docs/
autonomous: true
requirements:
  - SUB-03
must_haves:
  truths:
    - "Only files proven unused by reference checks can be removed."
    - "Active source folders must not be ignored by git."
  artifacts:
    - path: .gitignore
      provides: "Tracked source policy for cleanup safety"
    - path: scripts/
      provides: "Only actively used script inventory"
  key_links:
    - from: .gitignore
      to: remotion/
      via: "ignore policy"
      pattern: "remotion source is tracked"
---

<objective>
Create an evidence-backed file cleanup pass that removes dead files/scripts/artifacts while preserving all active submission and demo paths.

Output: cleaned repository file graph and adjusted ignore policy.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-codebase-cleanup/06-RESEARCH.md
@.planning/phases/06-codebase-cleanup/06-VALIDATION.md
@.gitignore
@scripts
@docs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build dead-file inventory with reference proof</name>
  <read_first>
    - .planning/phases/06-codebase-cleanup/06-RESEARCH.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>
    - scripts/
    - docs/
    - .planning/
  </files>
  <action>
    Produce a keep/remove inventory where every removal candidate has an explicit zero-reference proof from code and docs search.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; git ls-files | Out-Null; git ls-files --others --ignored --exclude-standard | Out-Null; Write-Host 'inventory-proof-ok'"</automated>
  </verify>
  <acceptance_criteria>
    No removal candidate is listed without a reference proof.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Align ignore rules with active source usage</name>
  <read_first>
    - .gitignore
    - package.json
  </read_first>
  <files>
    - .gitignore
  </files>
  <action>
    Remove or narrow ignore patterns that hide active source files required by build/demo workflows, including remotion source tracking.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; if (Select-String -Path '.gitignore' -Pattern '^/remotion$' -Quiet) { throw 'remotion-still-ignored' }; Write-Host 'ignore-policy-ok'"</automated>
  </verify>
  <acceptance_criteria>
    Active source paths are tracked and visible in diffs.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Apply approved dead-file removals and produce diff rationale</name>
  <read_first>
    - inventory output from Task 1
  </read_first>
  <files>
    - scripts/
    - docs/
    - .planning/phases/06-codebase-cleanup/
  </files>
  <action>
    Remove only approved dead files and add a short rationale section in summary for each removal.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; git diff --name-status | Out-Null; Write-Host 'cleanup-diff-ok'"</automated>
  </verify>
  <acceptance_criteria>
    Each removed file has a documented rationale and no active references remain.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- powershell -NoProfile -Command "$ErrorActionPreference='Stop'; npm run lint | Out-Null; Write-Host 'phase-06-01-lint-gate-ok'"
</verification>

<success_criteria>
- Dead files are removed only with proof.
- Active source directories are not hidden by ignore rules.
- Cleanup diff is explainable and auditable.
</success_criteria>

<output>
After completion, create .planning/phases/06-codebase-cleanup/06-01-SUMMARY.md.
</output>
