---
phase: 06-codebase-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - .planning/phases/06-codebase-cleanup/06-03-SUMMARY.md
autonomous: true
requirements:
  - SUB-03
must_haves:
  truths:
    - "Cleanup is complete only if all release-adjacent checks pass."
    - "No regression claims are valid without command-backed evidence."
  artifacts:
    - path: .planning/phases/06-codebase-cleanup/06-03-SUMMARY.md
      provides: "Regression-proof evidence bundle"
---

<objective>
Run full no-regression gates after cleanup and produce execution evidence proving readiness for final release validation.

Output: green gate evidence summary for Phase 7 handoff.
</objective>

<context>
@.planning/phases/06-codebase-cleanup/06-VALIDATION.md
@package.json
@hardhat.config.js
@remotion/Root.tsx
@scripts/testnet-config-check.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Validate static quality gate</name>
  <read_first>
    - package.json
  </read_first>
  <files>
    - src/
  </files>
  <action>
    Run lint and ensure cleanup has not introduced static analysis regressions.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; npm run lint | Out-Null; Write-Host 'lint-gate-ok'"</automated>
  </verify>
  <acceptance_criteria>
    Lint passes without new errors.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Validate build and contract checks</name>
  <read_first>
    - package.json
    - hardhat.config.js
  </read_first>
  <files>
    - contracts/
    - src/
  </files>
  <action>
    Run build and contract compile/test commands to prove no functional regressions.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; npm run build | Out-Null; npm run contracts:compile | Out-Null; npm run contracts:test | Out-Null; Write-Host 'build-contract-gates-ok'"</automated>
  </verify>
  <acceptance_criteria>
    Build and contract checks are green.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Validate testnet and demo contract continuity</name>
  <read_first>
    - scripts/testnet-config-check.js
    - remotion/Root.tsx
  </read_first>
  <files>
    - scripts/
    - remotion/
  </files>
  <action>
    Run testnet configuration verification and confirm demo composition is still discoverable.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; npm run testnet:config-check | Out-Null; npx remotion compositions remotion/index.ts | Select-String -Pattern 'HackathonDemo90' | Out-Null; Write-Host 'testnet-demo-continuity-ok'"</automated>
  </verify>
  <acceptance_criteria>
    Testnet config and remotion composition checks pass.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- powershell -NoProfile -Command "$ErrorActionPreference='Stop'; npm run lint | Out-Null; npm run build | Out-Null; Write-Host 'phase-06-03-final-gates-ok'"
</verification>

<success_criteria>
- All cleanup-related regression gates are green.
- Evidence is captured for Phase 7 release validation.
</success_criteria>

<output>
After completion, create .planning/phases/06-codebase-cleanup/06-03-SUMMARY.md.
</output>
