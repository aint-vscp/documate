---
phase: 06-codebase-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - SUB-03
must_haves:
  truths:
    - "Dependency removals require usage evidence and lockfile consistency."
    - "No package can be removed if it is required by runtime, build, test, or demo scripts."
  artifacts:
    - path: package.json
      provides: "Pruned dependency graph"
    - path: package-lock.json
      provides: "Installable lockfile after cleanup"
---

<objective>
Prune unused dependencies and keep the lockfile healthy without breaking build, test, or demo execution.

Output: minimal dependency graph with reproducible install state.
</objective>

<context>
@.planning/phases/06-codebase-cleanup/06-RESEARCH.md
@.planning/phases/06-codebase-cleanup/06-VALIDATION.md
@package.json
@package-lock.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Generate dependency usage report</name>
  <read_first>
    - package.json
  </read_first>
  <files>
    - package.json
  </files>
  <action>
    Run a dependency usage scanner and classify findings into removable, required, and uncertain buckets.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; npx depcheck --json | Out-Null; Write-Host 'dep-report-ok'"</automated>
  </verify>
  <acceptance_criteria>
    Cleanup candidates are evidence-backed and reviewed before removal.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Prune approved dependencies and refresh lockfile</name>
  <read_first>
    - dep report output
  </read_first>
  <files>
    - package.json
    - package-lock.json
  </files>
  <action>
    Remove only approved dependency entries, then run install to regenerate a coherent lockfile.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; npm prune | Out-Null; npm install | Out-Null; Write-Host 'prune-install-ok'"</automated>
  </verify>
  <acceptance_criteria>
    package.json and lockfile are synchronized after pruning.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Validate top-level dependency graph integrity</name>
  <read_first>
    - package.json
    - package-lock.json
  </read_first>
  <files>
    - package.json
    - package-lock.json
  </files>
  <action>
    Validate dependency integrity and flag unmet or invalid top-level entries.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; npm ls --depth=0 | Out-Null; Write-Host 'dep-integrity-ok'"</automated>
  </verify>
  <acceptance_criteria>
    npm ls completes without unmet dependency errors.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- powershell -NoProfile -Command "$ErrorActionPreference='Stop'; npm run lint | Out-Null; Write-Host 'phase-06-02-lint-gate-ok'"
</verification>

<success_criteria>
- Unused dependencies are removed with evidence.
- Lockfile remains valid and installable.
- No dependency graph breakage is introduced.
</success_criteria>

<output>
After completion, create .planning/phases/06-codebase-cleanup/06-02-SUMMARY.md.
</output>
