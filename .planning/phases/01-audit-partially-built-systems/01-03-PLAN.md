---
phase: 01-audit-partially-built-systems
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/audit/01-03-reputation-derivation-evidence.md
  - docs/audit/01-03-reputation-commands.txt
autonomous: true
requirements:
  - REP-01
  - REP-02
must_haves:
  truths:
    - "Profile-visible reputation tags are traced to their true origin (chain, DB, local, or mixed)."
    - "On-chain reputation scanner existence is separated from live UX wiring proof."
    - "Administrative/manual DB tag writes are explicitly distinguished from chain-derived tags."
  artifacts:
    - path: "docs/audit/01-03-reputation-derivation-evidence.md"
      provides: "Source-of-truth matrix and REP verdicts"
    - path: "docs/audit/01-03-reputation-commands.txt"
      provides: "Raw evidence transcript for tag derivation paths"
  key_links:
    - from: "src/app/api/reputation/[id]/route.ts"
      to: "src/app/dashboard/profile/page.tsx"
      via: "on-chain history API existence versus profile consumption"
      pattern: "fetchReputationHistory|/api/reputation|getReputationTagsForAddress"
    - from: "src/lib/indexer/handlers/poc.ts"
      to: "prisma/schema.prisma"
      via: "tag derivation and persistence completeness"
      pattern: "deriveTagsFromPOC|reputationTag.create"
---

<objective>
Prove whether reputation tags are derived from on-chain history or primarily from DB/local heuristics.

Purpose: Establish REP-01 and REP-02 trust boundaries before any remediation work.
Output: Source-of-truth evidence report + raw command transcript with explicit PASS/FAIL/UNVERIFIED outcomes.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-audit-partially-built-systems/01-RESEARCH.md
@src/app/api/reputation/[id]/route.ts
@src/lib/polkadot/assetHub.ts
@src/lib/reputation/tagging.ts
@src/lib/indexer/handlers/poc.ts
@src/app/dashboard/profile/page.tsx
@src/app/api/directory/tags/route.ts
@src/app/api/admin/breaches/route.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture chain-source, DB-source, and local-source evidence for reputation tags</name>
  <read_first>
    src/app/api/reputation/[id]/route.ts
    src/app/dashboard/profile/page.tsx
    src/lib/reputation/tagging.ts
    src/lib/indexer/handlers/poc.ts
  </read_first>
  <files>docs/audit/01-03-reputation-commands.txt</files>
  <action>
    Execute read-only PowerShell commands and store full output in docs/audit/01-03-reputation-commands.txt.

    $ts = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
    "=== 01-03 audit run: $ts ===" | Out-File -FilePath .\docs\audit\01-03-reputation-commands.txt -Encoding utf8

    Get-ChildItem -Path .\src -Recurse -File |
      Select-String -Pattern 'fetchReputationHistory|/api/reputation/\[id\]|scan recent blocks|system\.remark' |
      Out-File -FilePath .\docs\audit\01-03-reputation-commands.txt -Append -Encoding utf8

    Select-String -Path .\src\app\dashboard\profile\page.tsx -Pattern 'getReputationTagsForAddress|setReputationTags|/api/reputation' |
      Out-File -FilePath .\docs\audit\01-03-reputation-commands.txt -Append -Encoding utf8

    Get-ChildItem -Path .\src -Recurse -File |
      Select-String -Pattern 'addReputationTagsForAddress|/api/directory/tags|tagsJson|reputationTag\.create' |
      Out-File -FilePath .\docs\audit\01-03-reputation-commands.txt -Append -Encoding utf8

    Select-String -Path .\src\lib\indexer\handlers\poc.ts -Pattern 'deriveTagsFromPOC|reputationTag.create|await prisma' |
      Out-File -FilePath .\docs\audit\01-03-reputation-commands.txt -Append -Encoding utf8

    Select-String -Path .\src\lib\reputation\tagging.ts -Pattern 'deriveReputationTags|templateName|placeholderValues|localStorage' |
      Out-File -FilePath .\docs\audit\01-03-reputation-commands.txt -Append -Encoding utf8
  </action>
  <verify>
    <automated>Test-Path .\docs\audit\01-03-reputation-commands.txt; if ($?) { Get-Content .\docs\audit\01-03-reputation-commands.txt | Measure-Object -Line }</automated>
  </verify>
  <acceptance_criteria>
    Transcript contains evidence for chain scanner, profile tag source, local/DB write paths, and indexer persistence status.
  </acceptance_criteria>
  <done>
    All derivation claims are backed by captured command evidence.
  </done>
</task>

<task type="auto">
  <name>Task 2: Publish source-of-truth matrix and REP verdicts with anti-false-proof gates</name>
  <read_first>
    docs/audit/01-03-reputation-commands.txt
    .planning/phases/01-audit-partially-built-systems/01-RESEARCH.md
  </read_first>
  <files>docs/audit/01-03-reputation-derivation-evidence.md</files>
  <action>
    Create docs/audit/01-03-reputation-derivation-evidence.md with:
    - Scope guardrail: "Phase 1 audit only, no implementation/fix work."
    - Source-of-truth matrix columns: Surface, Current Source, Chain Evidence Present, Evidence Snippet, Verdict.
    - Separate verdict lines for REP-01 and REP-02.
    - PASS/FAIL/UNVERIFIED policy:
      - PASS only when profile-visible tags are demonstrably chain-derived with traceable tx/block evidence.
      - FAIL when visible tags are primarily local/DB/manual without chain linkage.
      - UNVERIFIED when partial path exists but end-to-end evidence chain is incomplete.
    - Remediation pointers as "Phase 2 candidate gaps" only (no implementation steps).
  </action>
  <verify>
    <automated>Select-String -Path .\docs\audit\01-03-reputation-derivation-evidence.md -Pattern 'PASS|FAIL|UNVERIFIED|REP-01|REP-02|Source-of-truth matrix|Phase 1 audit only' -CaseSensitive</automated>
  </verify>
  <acceptance_criteria>
    Both REP requirements have explicit evidence-backed outcomes and no PASS verdict is granted without chain-origin proof.
  </acceptance_criteria>
  <done>
    Reputation derivation truth boundary is unambiguous for planning Phase 2 fixes.
  </done>
</task>

</tasks>

<verification>
Overall checks:
1) Transcript includes all five evidence command groups.
2) Evidence report distinguishes chain, DB, local, and mixed sources.
3) Phase 1 scope is preserved (audit artifacts only; no feature changes).
</verification>

<success_criteria>
A third party can reproduce REP-01 and REP-02 verdicts from command transcript plus source-of-truth matrix.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit-partially-built-systems/01-03-SUMMARY.md`.
</output>
