---
phase: 01-audit-partially-built-systems
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/audit/01-02-tee-tier-ux-evidence.md
  - docs/audit/01-02-tee-tier-commands.txt
autonomous: true
requirements:
  - DOC-03
must_haves:
  truths:
    - "TEE Gold/Silver/Bronze visibility is evaluated from user-facing routes, not route comments alone."
    - "Backend tier logic and frontend endpoint consumption are both evidenced with command output."
    - "UNVERIFIED is used when tier payload existence is known but UI linkage cannot be proven."
  artifacts:
    - path: "docs/audit/01-02-tee-tier-ux-evidence.md"
      provides: "Route-by-route UX visibility verdict"
    - path: "docs/audit/01-02-tee-tier-commands.txt"
      provides: "Raw command transcript for endpoint/UI evidence"
  key_links:
    - from: "src/app/api/validate-document/route.ts"
      to: "src/app/dashboard/filing/page.tsx"
      via: "endpoint call path and rendered tier evidence"
      pattern: "Gold|Silver|Bronze|/api/validate-document"
    - from: "src/app/api/phala-proxy/route.ts"
      to: "src/app/dashboard/filing/page.tsx"
      via: "proxy mode and user-facing TEE messaging"
      pattern: "mode: \"mock\"|TEE:"
---

<objective>
Prove whether Gold/Silver/Bronze TEE output is truly surfaced in user-facing UX flows.

Purpose: Establish DOC-03 reality using evidence from code paths and UI wiring only.
Output: Evidence report + command transcript with explicit PASS/FAIL/UNVERIFIED result.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-audit-partially-built-systems/01-RESEARCH.md
@src/app/api/validate-document/route.ts
@src/app/dashboard/filing/page.tsx
@src/app/api/phala-proxy/route.ts
@src/app/dashboard/documents/[id]/page.tsx
@src/components/document/SignaturePanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gather backend-tier and frontend-consumption evidence</name>
  <read_first>
    src/app/api/validate-document/route.ts
    src/app/dashboard/filing/page.tsx
    src/app/api/phala-proxy/route.ts
  </read_first>
  <files>docs/audit/01-02-tee-tier-commands.txt</files>
  <action>
    Run read-only PowerShell commands and capture raw output to docs/audit/01-02-tee-tier-commands.txt.

    $ts = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
    "=== 01-02 audit run: $ts ===" | Out-File -FilePath .\docs\audit\01-02-tee-tier-commands.txt -Encoding utf8

    Select-String -Path .\src\app\api\validate-document\route.ts -Pattern 'Gold|Silver|Bronze|tierName|tierColor|attestation' |
      Out-File -FilePath .\docs\audit\01-02-tee-tier-commands.txt -Append -Encoding utf8

    Get-ChildItem -Path .\src -Recurse -File |
      Select-String -Pattern '/api/validate-document|validate-document' -SimpleMatch |
      Out-File -FilePath .\docs\audit\01-02-tee-tier-commands.txt -Append -Encoding utf8

    Select-String -Path .\src\app\dashboard\filing\page.tsx -Pattern 'TEE:|Mock|Live|tier|Gold|Silver|Bronze|attestation' |
      Out-File -FilePath .\docs\audit\01-02-tee-tier-commands.txt -Append -Encoding utf8

    Select-String -Path .\src\app\api\phala-proxy\route.ts -Pattern 'mode: "mock"|mock mode|In production' |
      Out-File -FilePath .\docs\audit\01-02-tee-tier-commands.txt -Append -Encoding utf8
  </action>
  <verify>
    <automated>Test-Path .\docs\audit\01-02-tee-tier-commands.txt; if ($?) { Get-Content .\docs\audit\01-02-tee-tier-commands.txt | Measure-Object -Line }</automated>
  </verify>
  <acceptance_criteria>
    Transcript includes evidence of both tier-generation logic and whether user-facing files consume/render it.
  </acceptance_criteria>
  <done>
    No assumptions about UX visibility are made without file/path evidence in transcript.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write route-by-route UX verdict with strict PASS/FAIL/UNVERIFIED gating</name>
  <read_first>
    docs/audit/01-02-tee-tier-commands.txt
    .planning/phases/01-audit-partially-built-systems/01-RESEARCH.md
  </read_first>
  <files>docs/audit/01-02-tee-tier-ux-evidence.md</files>
  <action>
    Create docs/audit/01-02-tee-tier-ux-evidence.md containing:
    - Scope guardrail: "Audit only in Phase 1; no implementation changes."
    - A route visibility table with columns: Route/File, Tier Data Source, Rendered Fields, Evidence Snippet, Verdict.
    - Verdict policy:
      - PASS: Gold/Silver/Bronze is rendered in at least one normal user flow and tied to live response payload.
      - FAIL: Tier exists only server-side or in non-rendered code paths.
      - UNVERIFIED: Evidence incomplete for proving user-visible tier linkage.
    - Explicit DOC-03 status and a concise blocked-by list if not PASS.
  </action>
  <verify>
    <automated>Select-String -Path .\docs\audit\01-02-tee-tier-ux-evidence.md -Pattern 'PASS|FAIL|UNVERIFIED|route visibility table|Audit only in Phase 1|DOC-03' -CaseSensitive</automated>
  </verify>
  <acceptance_criteria>
    Report shows a clear result for DOC-03 and avoids claiming UX coverage without command-cited file evidence.
  </acceptance_criteria>
  <done>
    TEE tier surfacing status is documented with reproducible proof and no false-positive language.
  </done>
</task>

</tasks>

<verification>
Overall checks:
1) Raw command transcript captures tier logic and route consumption search results.
2) Report includes deterministic PASS/FAIL/UNVERIFIED rubric.
3) Scope remains audit-only with zero feature implementation.
</verification>

<success_criteria>
Independent reviewers can confirm the DOC-03 verdict by re-running the listed PowerShell commands.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit-partially-built-systems/01-02-SUMMARY.md`.
</output>
