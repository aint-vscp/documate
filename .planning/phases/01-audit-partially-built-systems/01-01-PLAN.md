---
phase: 01-audit-partially-built-systems
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/audit/01-01-document-anchor-evidence.md
  - docs/audit/01-01-document-anchor-commands.txt
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "Document anchor claims are classified as on-chain proven, DB-backed only, UI-only, or mixed."
    - "No PASS result is granted without concrete chain evidence tied to a tx hash or event."
    - "Fallback marker usage (offchain-anchor:*) is explicitly detected and reported."
  artifacts:
    - path: "docs/audit/01-01-document-anchor-evidence.md"
      provides: "Evidence table, trust-boundary matrix, and verdict"
    - path: "docs/audit/01-01-document-anchor-commands.txt"
      provides: "Raw command outputs used as proof"
  key_links:
    - from: "src/app/dashboard/documents/[id]/page.tsx"
      to: "contracts/DocuMate.sol"
      via: "uploadDocument(finalHash) write path and fallback path"
      pattern: "uploadDocument\(|offchain-anchor"
    - from: "src/app/api/activity/route.ts"
      to: "src/app/api/documents/route.ts"
      via: "transactionHash rendered in activity and sourced from shared payload"
      pattern: "DOCUMENT_ANCHOR|transactionHash"
---

<objective>
Prove whether document hash anchoring is truly on-chain or only represented in DB/UI artifacts.

Purpose: Produce an audit-grade, evidence-first verdict for DOC-01 before any implementation work.
Output: Evidence report + raw command transcript with explicit PASS/FAIL/UNVERIFIED result.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-audit-partially-built-systems/01-RESEARCH.md
@contracts/DocuMate.sol
@src/app/dashboard/documents/[id]/page.tsx
@src/app/api/documents/route.ts
@src/app/api/activity/route.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collect immutable evidence for contract path, fallback path, and DB payload path</name>
  <read_first>
    contracts/DocuMate.sol
    src/app/dashboard/documents/[id]/page.tsx
    src/app/api/documents/route.ts
    src/app/api/activity/route.ts
  </read_first>
  <files>docs/audit/01-01-document-anchor-commands.txt</files>
  <action>
    Execute only read-only PowerShell audit commands and append full stdout/stderr to docs/audit/01-01-document-anchor-commands.txt.

    $ts = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
    "=== 01-01 audit run: $ts ===" | Out-File -FilePath .\docs\audit\01-01-document-anchor-commands.txt -Encoding utf8

    Get-ChildItem -Path .\contracts -Filter *.sol -Recurse |
      Select-String -Pattern 'function uploadDocument|event DocumentUploaded|getDocument\(' -CaseSensitive |
      Out-File -FilePath .\docs\audit\01-01-document-anchor-commands.txt -Append -Encoding utf8

    Get-ChildItem -Path .\src -Recurse -File |
      Select-String -Pattern 'uploadDocument\(|offchain-anchor|Anchoring document hash on-chain|On-chain anchoring unavailable' |
      Out-File -FilePath .\docs\audit\01-01-document-anchor-commands.txt -Append -Encoding utf8

    Get-ChildItem -Path .\src\app\api -Recurse -File |
      Select-String -Pattern 'SharedDocument|payload|transactionHash|\$executeRawUnsafe|\$queryRawUnsafe' |
      Out-File -FilePath .\docs\audit\01-01-document-anchor-commands.txt -Append -Encoding utf8

    Select-String -Path .\src\app\api\activity\route.ts -Pattern 'DOCUMENT_ANCHOR|transactionHash|\^0x\[a-fA-F0-9\]\{64\}\$' |
      Out-File -FilePath .\docs\audit\01-01-document-anchor-commands.txt -Append -Encoding utf8

    Get-ChildItem -Path . -Recurse -Include *.json,*.log,*.txt |
      Select-String -Pattern 'offchain-anchor:' -SimpleMatch |
      Out-File -FilePath .\docs\audit\01-01-document-anchor-commands.txt -Append -Encoding utf8
  </action>
  <verify>
    <automated>Test-Path .\docs\audit\01-01-document-anchor-commands.txt; if ($?) { Get-Content .\docs\audit\01-01-document-anchor-commands.txt | Measure-Object -Line }</automated>
  </verify>
  <acceptance_criteria>
    Command transcript exists and contains outputs for all five command groups.
  </acceptance_criteria>
  <done>
    Evidence source is raw command output only; no inferred claims without command evidence.
  </done>
</task>

<task type="auto">
  <name>Task 2: Produce verdict matrix with PASS/FAIL/UNVERIFIED classification rules</name>
  <read_first>
    docs/audit/01-01-document-anchor-commands.txt
    .planning/phases/01-audit-partially-built-systems/01-RESEARCH.md
  </read_first>
  <files>docs/audit/01-01-document-anchor-evidence.md</files>
  <action>
    Create docs/audit/01-01-document-anchor-evidence.md with:
    - Scope statement: "Phase 1 audit only, no fixes implemented."
    - Evidence matrix columns: Claim, Source File, Command Evidence, Trust Class, Verdict.
    - Trust classes: On-chain proven, DB-backed only, UI-only, Mixed.
    - PASS/FAIL/UNVERIFIED rubric:
      - PASS only if chain proof exists (event/storage/tx-level evidence) and no fallback equivalence in finalized path.
      - FAIL if fallback or non-chain path is treated as equivalent proof.
      - UNVERIFIED if required runtime chain proof cannot be established from available evidence.
    - Final verdict for DOC-01 with explicit reason and blocked-by list if UNVERIFIED.
  </action>
  <verify>
    <automated>Select-String -Path .\docs\audit\01-01-document-anchor-evidence.md -Pattern 'PASS|FAIL|UNVERIFIED|On-chain proven|DB-backed only|UI-only|Mixed|Phase 1 audit only' -CaseSensitive</automated>
  </verify>
  <acceptance_criteria>
    Report includes at least one explicit PASS/FAIL/UNVERIFIED result and never labels any item PASS without cited command evidence.
  </acceptance_criteria>
  <done>
    DOC-01 status is evidence-backed with no false-proof language.
  </done>
</task>

</tasks>

<verification>
Overall checks:
1) Transcript file exists with raw outputs from all planned commands.
2) Evidence report contains trust-boundary table and verdict logic.
3) Scope guardrail enforced: no source code or behavior changes, audit artifacts only.
</verification>

<success_criteria>
A reviewer can reproduce the verdict by running the documented PowerShell commands and obtaining materially similar evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit-partially-built-systems/01-01-SUMMARY.md`.
</output>
