---
phase: 05-remotion-demo-video
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - remotion/Root.tsx
  - remotion/HackathonDemoVideo.tsx
  - docs/demo-script.md
autonomous: true
requirements:
  - SUB-02
  - DOC-01
  - DOC-03
  - REP-01
  - REP-02
  - SET-03
must_haves:
  truths:
    - "The demo composition is exactly 90 seconds (2700 frames at 30fps)."
    - "The video narratively covers five systems in sequence before the final proof hold."
    - "No spoken or on-screen claim is allowed unless the exact proof text is present in source."
  artifacts:
    - path: remotion/Root.tsx
      provides: "Canonical 90-second composition registration for the demo"
    - path: remotion/HackathonDemoVideo.tsx
      provides: "Frame-locked sequence timeline for five-system story"
    - path: docs/demo-script.md
      provides: "Narration aligned to frame/time segments"
  key_links:
    - from: remotion/Root.tsx
      to: remotion/HackathonDemoVideo.tsx
      via: "Composition id and durationInFrames mapping"
      pattern: "Composition id=HackathonDemo90 with durationInFrames=2700"
    - from: remotion/HackathonDemoVideo.tsx
      to: docs/demo-script.md
      via: "Sequence order and timestamps"
      pattern: "Identity -> Document Anchor -> TEE -> Settlement -> Reputation"
---

<objective>
Create the executable 90-second storyline contract for Phase 5 so the demo reliably presents all five systems in strict sequence before the live-proof ending.

Purpose: Lock timing and story order to prevent narrative drift and unverifiable claims.
Output: Updated Remotion composition metadata, frame timeline, and synchronized demo script.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-remotion-demo-video/05-RESEARCH.md
@.planning/phases/05-remotion-demo-video/05-VALIDATION.md
@package.json
@remotion/Root.tsx
@remotion/HackathonDemoVideo.tsx
@docs/demo-script.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Establish 90-second composition contract in Remotion root</name>
  <read_first>
    - .planning/phases/05-remotion-demo-video/05-RESEARCH.md
    - remotion/Root.tsx
    - package.json
  </read_first>
  <files>
    - remotion/Root.tsx
    - package.json
  </files>
  <action>
    Register a canonical 90-second demo composition id (`HackathonDemo90`) at 30fps, 1920x1080, 2700 frames, and align `scripts.video:render` to this canonical id.

    Anti-hallucination rules for this task:
    - Do not claim "90-second demo" unless `durationInFrames` is exactly `2700` and fps is exactly `30`.
    - Do not keep ambiguous id naming for final flow; canonical id must match render script.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; Select-String -Path 'remotion/Root.tsx' -Pattern 'id=\"HackathonDemo90\"|durationInFrames=\{2700\}|fps=\{30\}|width=\{1920\}|height=\{1080\}' | Out-Null; (Get-Content package.json -Raw) | Select-String -Pattern '\"video:render\"\s*:\s*\"npx remotion render HackathonDemo90 docs/demo-video.mp4\"' | Out-Null; Write-Host 'composition-contract-ok'"</automated>
  </verify>
  <acceptance_criteria>
    `HackathonDemo90` is the authoritative composition id and render script target for Phase 5.
  </acceptance_criteria>
  <done>
    Composition metadata and render entrypoint are deterministic and internally consistent.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement five-system 90-second frame ledger and sequence order</name>
  <read_first>
    - .planning/phases/05-remotion-demo-video/05-RESEARCH.md
    - remotion/HackathonDemoVideo.tsx
  </read_first>
  <files>
    - remotion/HackathonDemoVideo.tsx
  </files>
  <action>
    Build a frame-locked sequence ledger totaling exactly 2700 frames with five systems in order:
    1) Identity precompile gate,
    2) Document anchor trust layer,
    3) TEE Gold/Silver/Bronze visibility,
    4) Deterministic settlement split (75/20/5),
    5) Reputation and slashing accountability,
    followed by final live-proof segment start at frame 1800.

    Anti-hallucination rules for this task:
    - Do not imply a system is covered unless its segment label exists in source and in script.
    - Do not claim deterministic split unless explicit `75/20/5` appears on-screen.
    - Do not claim runtime identity integration unless `0x0000000000000000000000000000000000000818` is visible.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; Select-String -Path 'remotion/HackathonDemoVideo.tsx' -Pattern 'Sequence from=\{0\}|Sequence from=\{300\}|Sequence from=\{600\}|Sequence from=\{900\}|Sequence from=\{1200\}|Sequence from=\{1500\}|Sequence from=\{1800\}|durationInFrames=\{900\}|75/20/5|0x0000000000000000000000000000000000000818' | Out-Null; Write-Host 'timeline-ledger-ok'"</automated>
  </verify>
  <acceptance_criteria>
    The source contains an explicit, ordered frame contract for five systems and the handoff to final proof hold.
  </acceptance_criteria>
  <done>
    Timeline order is deterministic and aligns to phase scope boundaries.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Synchronize demo narration with frame sequence and evidence-safe claims</name>
  <read_first>
    - docs/demo-script.md
    - remotion/HackathonDemoVideo.tsx
    - .planning/phases/05-remotion-demo-video/05-RESEARCH.md
  </read_first>
  <files>
    - docs/demo-script.md
  </files>
  <action>
    Rewrite `docs/demo-script.md` into a 90-second script that mirrors the exact five-system order from source and references only visible proof claims.
    Add a compact "Claim Guardrails" subsection enforcing that any claim must map to visible literals in the video source.

    Anti-hallucination rules for this task:
    - No unverifiable wording such as "fully live" unless addresses and links are visibly shown.
    - No timing claims unless mapped to explicit timestamp blocks that sum to 90 seconds.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; Select-String -Path 'docs/demo-script.md' -Pattern '90-second|0:00|0:10|0:20|0:30|0:40|0:50|1:00|Claim Guardrails|75/20/5|0x0000000000000000000000000000000000000818' | Out-Null; Write-Host 'script-sync-ok'"</automated>
  </verify>
  <acceptance_criteria>
    Script timeline and source timeline are aligned and claim language is evidence-gated.
  </acceptance_criteria>
  <done>
    Judge-facing narration cannot drift from what is provably visible on screen.
  </done>
</task>

</tasks>

<verification>
Run quick checks first (<=30s each):
- powershell -NoProfile -Command "$ErrorActionPreference='Stop'; npx remotion compositions remotion/index.ts | Select-String -Pattern 'HackathonDemo90\s+30\s+1920x1080\s+2700' | Out-Null; Write-Host 'composition-list-ok'"

Run full timing gate after task-level checks:
- powershell -NoProfile -Command "$ErrorActionPreference='Stop'; npx remotion render HackathonDemo90 docs/demo-video.mp4 --codec=h264 --crf=18 --pixel-format=yuv420p --concurrency=1 --disallow-parallel-encoding --overwrite | Out-Null; if (!(Test-Path 'docs/demo-video.mp4')) { throw 'missing-output' }; Write-Host 'phase-05-01-render-gate-ok'"
</verification>

<success_criteria>
- Storyline is 90 seconds and frame-locked.
- Five systems appear in strict sequence before final proof hold.
- All demo claims are literal-backed and timing-backed in source/docs.
</success_criteria>

<output>
After completion, create `.planning/phases/05-remotion-demo-video/05-01-SUMMARY.md`.
</output>
