---
phase: 05-remotion-demo-video
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - package.json
  - scripts/verify-demo-render.ps1
  - docs/demo-script.md
autonomous: true
requirements:
  - SUB-02
  - SUB-03
must_haves:
  truths:
    - "A single npm command reproducibly renders the official demo artifact."
    - "Artifact checks can prove output existence, size, and hash deterministically."
    - "Render verification instructions are executable on Windows PowerShell without manual interpretation."
  artifacts:
    - path: package.json
      provides: "Canonical npm render script for demo artifact"
    - path: scripts/verify-demo-render.ps1
      provides: "Deterministic reproducibility and artifact validation routine"
    - path: docs/demo-script.md
      provides: "Operator-visible render verification procedure for submission"
  key_links:
    - from: package.json
      to: scripts/verify-demo-render.ps1
      via: "Same render command contract"
      pattern: "video:render command and flags parity"
    - from: scripts/verify-demo-render.ps1
      to: docs/demo-video.mp4
      via: "artifact existence and SHA256 checks"
      pattern: "Test-Path + Get-FileHash"
---

<objective>
Deliver reproducible demo rendering with deterministic artifact checks so SUB-02 output and SUB-03 evidence can be regenerated from source at will.

Purpose: Convert demo production into a repeatable verification pipeline rather than a one-off export.
Output: Stable npm render command, PowerShell verification script, and documented checks.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-remotion-demo-video/05-RESEARCH.md
@.planning/phases/05-remotion-demo-video/05-02-PLAN.md
@package.json
@docs/demo-script.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Canonicalize render command contract in npm scripts</name>
  <read_first>
    - package.json
    - .planning/phases/05-remotion-demo-video/05-RESEARCH.md
  </read_first>
  <files>
    - package.json
  </files>
  <action>
    Ensure `scripts.video:render` is the official, reproducible entrypoint for rendering `docs/demo-video.mp4` from `HackathonDemo90`.
    Keep command deterministic and compatible with Windows PowerShell execution.

    Anti-hallucination rules for this task:
    - Do not document or use alternative hidden render commands as authoritative.
    - Do not claim reproducibility unless the canonical command is singular and explicit.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; (Get-Content package.json -Raw) | Select-String -Pattern '\"video:render\"\s*:\s*\"npx remotion render HackathonDemo90 docs/demo-video.mp4\"' | Out-Null; Write-Host 'video-render-script-ok'"</automated>
  </verify>
  <acceptance_criteria>
    `npm run video:render` alone identifies the official artifact generation path.
  </acceptance_criteria>
  <done>
    Render command contract is stable and unambiguous.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add deterministic PowerShell artifact verification script</name>
  <read_first>
    - .planning/phases/05-remotion-demo-video/05-VALIDATION.md
    - package.json
  </read_first>
  <files>
    - scripts/verify-demo-render.ps1
  </files>
  <action>
    Create `scripts/verify-demo-render.ps1` that:
    1) runs `npm run video:render` with deterministic flags,
    2) asserts `docs/demo-video.mp4` exists,
    3) records file size and SHA256,
    4) optionally supports a second-run hash compare mode (`-Repeat`) for reproducibility evidence.

    Anti-hallucination rules for this task:
    - Do not report "reproducible" unless hash comparison logic is actually implemented.
    - Do not silently pass on missing artifacts or failed render exit codes.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; if (!(Test-Path 'scripts/verify-demo-render.ps1')) { throw 'missing-script' }; Select-String -Path 'scripts/verify-demo-render.ps1' -Pattern 'npm run video:render|Get-FileHash|Test-Path|docs/demo-video.mp4|param\(\[switch\]\$Repeat\)' | Out-Null; Write-Host 'verify-script-shape-ok'"</automated>
  </verify>
  <acceptance_criteria>
    Verification script codifies deterministic render and artifact integrity checks.
  </acceptance_criteria>
  <done>
    SUB-03 has executable proof automation, not manual checklist prose.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Document reproducibility procedure and evidence capture outputs</name>
  <read_first>
    - docs/demo-script.md
    - scripts/verify-demo-render.ps1
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>
    - docs/demo-script.md
  </files>
  <action>
    Add a concise "Render Reproducibility" section in `docs/demo-script.md` with exact PowerShell commands for single-run and repeat-run hash validation.
    Include expected artifacts captured for submission evidence (path, size, SHA256, timestamp).

    Anti-hallucination rules for this task:
    - Do not claim successful render in docs unless command shown is executable as written.
    - Do not include unverifiable expected values (fixed hash strings) in static docs.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; Select-String -Path 'docs/demo-script.md' -Pattern 'Render Reproducibility|npm run video:render|scripts/verify-demo-render.ps1|SHA256|docs/demo-video.mp4' | Out-Null; Write-Host 'docs-repro-procedure-ok'"</automated>
  </verify>
  <acceptance_criteria>
    Submission operators can execute one documented command path for reproducibility evidence.
  </acceptance_criteria>
  <done>
    SUB-02 and SUB-03 evidence workflow is operationally documented.
  </done>
</task>

</tasks>

<verification>
Task-level checks stay lightweight (<=30s) and do not run full render by default.

Plan-level heavy verification:
1) powershell -NoProfile -Command "$ErrorActionPreference='Stop'; npm run video:render -- --codec=h264 --crf=18 --pixel-format=yuv420p --concurrency=1 --disallow-parallel-encoding --overwrite"
2) powershell -NoProfile -File "scripts/verify-demo-render.ps1"
3) powershell -NoProfile -File "scripts/verify-demo-render.ps1" -Repeat

Pass criteria:
- Render exits cleanly and writes `docs/demo-video.mp4`.
- Verification script emits size + SHA256 + timestamp.
- Repeat mode confirms deterministic hash match on same machine.
</verification>

<success_criteria>
- `npm run video:render` is the single canonical output path.
- Reproducibility checks are scriptable and deterministic on Windows PowerShell.
- Submission evidence for SUB-02 and SUB-03 can be regenerated on demand.
</success_criteria>

<output>
After completion, create `.planning/phases/05-remotion-demo-video/05-03-SUMMARY.md`.
</output>
