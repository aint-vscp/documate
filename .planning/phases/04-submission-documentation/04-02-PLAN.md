---
phase: 04-submission-documentation
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - docs/precompile-integration.md
  - docs/demo-script.md
  - docs/judge-qa.md
autonomous: true
requirements:
  - SUB-01
  - DOC-01
  - DOC-03
  - REP-01
  - REP-02
  - ID-01
  - ID-02
must_haves:
  truths:
    - Precompile integration proof document contains source-backed runtime call-path evidence and mock-default-off proof.
    - Demo script is executable in 3 minutes with deterministic fallback command path and expected outputs.
    - Judge Q and A answers are evidence-bound and do not rely on unverifiable claims.
  artifacts:
    - path: docs/precompile-integration.md
      provides: Track 2 runtime call-path proof and anti-hallucination evidence anchors
    - path: docs/demo-script.md
      provides: deterministic demo route, timing, and fallback evidence commands
    - path: docs/judge-qa.md
      provides: objection handling with direct source/command-backed answers
  key_links:
    - from: docs/precompile-integration.md
      to: contracts/DocuMateMarketplace.sol
      via: staticcall and selector proof references
      pattern: DEFAULT_IDENTITY_PRECOMPILE|identityPrecompile.staticcall|IIdentityPrecompile.identity.selector
    - from: docs/demo-script.md
      to: scripts/testnet-config-check.js
      via: fallback demo evidence path
      pattern: npm run testnet:config-check|Config check passed
    - from: docs/judge-qa.md
      to: SUBMISSION.md
      via: answer claims align with submission evidence checklist wording
      pattern: 75/20/5|Track 2|runtime precompile
---

<objective>
Finalize judge proof docs for precompile integration, demo script, and Q and A with deterministic evidence checks.

Purpose: Ensure supporting docs are synchronized, replayable, and resistant to claim drift.
Output: Three finalized docs that directly support SUB-01 and linked proof IDs through command-backed evidence.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-submission-documentation/04-RESEARCH.md
@.planning/phases/04-submission-documentation/04-VALIDATION.md
@.planning/phases/04-submission-documentation/04-01-PLAN.md
@README.md
@docs/precompile-integration.md
@docs/demo-script.md
@docs/judge-qa.md
@SUBMISSION.md
@contracts/DocuMateMarketplace.sol
@scripts/testnet-config-check.js
</context>

<scope_boundary>
Implement only docs/precompile-integration.md, docs/demo-script.md, and docs/judge-qa.md finalization with deterministic evidence checks for plan 04-02. Do not edit README.md or SUBMISSION.md in this plan.
</scope_boundary>

<tasks>

<task type="auto">
  <name>Task 1: Finalize precompile integration proof with strict source-evidence mapping</name>
  <read_first>
    - docs/precompile-integration.md
    - contracts/DocuMateMarketplace.sol
    - contracts/interfaces/IIdentityPrecompile.sol
  </read_first>
  <files>docs/precompile-integration.md</files>
  <action>
    Update precompile proof so each Track 2 claim is anchored to source evidence and deterministic command output. Include runtime precompile address, staticcall path, selector usage, and mock-default-off behavior with explicit verification references.

    Anti-hallucination rules for this task:
    - Do not claim successful runtime identity for any wallet unless output is shown from deterministic command.
    - Do not cite nonexistent line numbers, transactions, or deployment events.
    - Keep wording bounded to verifiable facts in contracts and scripts.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; Select-String -Path 'contracts/DocuMateMarketplace.sol' -Pattern 'DEFAULT_IDENTITY_PRECOMPILE|identityPrecompile\.staticcall|IIdentityPrecompile\.identity\.selector|useMockVerification\s*=\s*false' | Out-Null; $doc = Get-Content 'docs/precompile-integration.md' -Raw; foreach($required in @('0x0000000000000000000000000000000000000818','identityPrecompile.staticcall','IIdentityPrecompile.identity.selector','useMockVerification = false','420420417','0x233FE6112E5Ad4Db1c83358B30D581F837314BB1','0x1cf190eabe490B50AaBE91b4567ebe88126e8D24')) { if ($doc -notmatch [regex]::Escape($required)) { throw "precompile-integration missing: $required" } }</automated>
  </verify>
  <acceptance_criteria>
    - Precompile proof contains all required Track 2 source anchors.
    - Mock-mode and runtime-call assertions are source-backed and deterministic.
  </acceptance_criteria>
  <done>precompile-integration.md is final, reproducible, and evidence-consistent for ID-01 and ID-02 claims.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize demo script with deterministic timing, route sequence, and fallback evidence</name>
  <read_first>
    - docs/demo-script.md
    - scripts/testnet-config-check.js
    - README.md
  </read_first>
  <files>docs/demo-script.md</files>
  <action>
    Ensure the demo script has a strict 3-minute flow, concrete route sequence, and fallback command block with expected outputs for network/contract/split proof. Keep presenter language aligned to evidence already available in repo artifacts.

    Anti-hallucination rules for this task:
    - Do not include steps that require unproven outcomes.
    - Do not claim UI states that are not observable from existing routes/features.
    - Do not claim tx hash values unless they are explicitly captured and referenced.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $doc = Get-Content 'docs/demo-script.md' -Raw; foreach($required in @('0:00 - 0:20','0:20 - 0:45','0:45 - 1:20','1:20 - 2:00','2:00 - 2:35','2:35 - 3:00','/dashboard','/dashboard/studio','/dashboard/market','/dashboard/profile','/admin/breaches','npm run testnet:config-check','Config check passed','split(1 PAS)')) { if ($doc -notmatch [regex]::Escape($required)) { throw "demo-script missing: $required" } }; node scripts/testnet-config-check.js</automated>
  </verify>
  <acceptance_criteria>
    - Demo script can be replayed as a deterministic 3-minute flow.
    - Fallback command and expected output anchors are explicit and accurate.
  </acceptance_criteria>
  <done>demo-script.md is final and replayable with deterministic fallback proof.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize judge Q and A with evidence-bound answers and no speculative claims</name>
  <read_first>
    - docs/judge-qa.md
    - docs/precompile-integration.md
    - SUBMISSION.md
  </read_first>
  <files>docs/judge-qa.md</files>
  <action>
    Rewrite and tighten judge answers so each critical claim (Track 2 proof, deployed contracts, deterministic split, abuse enforcement, production posture) maps to a deterministic command or source file artifact. Keep responses concise and rebuttal-ready.

    Anti-hallucination rules for this task:
    - Replace inferred claims with bounded statements tied to evidence artifacts.
    - If a claim is pending or not evidenced, mark it as pending/NOT VERIFIED instead of asserting completion.
    - Keep all addresses and chain ID exactly aligned with README and SUBMISSION.md.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $doc = Get-Content 'docs/judge-qa.md' -Raw; foreach($required in @('0x0000000000000000000000000000000000000818','identityPrecompile.staticcall','IIdentityPrecompile.identity.selector','420420417','0x233FE6112E5Ad4Db1c83358B30D581F837314BB1','0x1cf190eabe490B50AaBE91b4567ebe88126e8D24','75/20/5','npm run testnet:config-check')) { if ($doc -notmatch [regex]::Escape($required)) { throw "judge-qa missing: $required" } }; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Judge Q and A responses are evidence-backed, concise, and non-speculative.
    - Critical values and claims are consistent with README/precompile/SUBMISSION artifacts.
  </acceptance_criteria>
  <done>judge-qa.md is final, evidence-grounded, and aligned with submission claims.</done>
</task>

</tasks>

<verification>
1. $ErrorActionPreference = 'Stop'; npm run lint
2. Select-String -Path 'contracts/DocuMateMarketplace.sol' -Pattern 'DEFAULT_IDENTITY_PRECOMPILE|identityPrecompile\.staticcall|IIdentityPrecompile\.identity\.selector|useMockVerification\s*=\s*false'
3. node scripts/testnet-config-check.js
4. $files = @('docs/precompile-integration.md','docs/demo-script.md','docs/judge-qa.md'); foreach($f in $files){ if (-not (Test-Path $f)) { throw "Missing required doc: $f" } }
5. $all = Get-Content 'docs/precompile-integration.md','docs/demo-script.md','docs/judge-qa.md' -Raw; foreach($v in @('420420417','0x233FE6112E5Ad4Db1c83358B30D581F837314BB1','0x1cf190eabe490B50AaBE91b4567ebe88126e8D24','0x0000000000000000000000000000000000000818')) { if ($all -notmatch [regex]::Escape($v)) { throw "Missing shared evidence value: $v" } }
</verification>

<success_criteria>
- 04-02 proof docs are finalized with deterministic evidence checks and anti-hallucination safeguards.
- SUB-01 support is complete across precompile proof, demo script, and judge Q and A narrative.
- Scope boundary respected: no README or SUBMISSION checklist edits are required in this plan.
</success_criteria>

<output>
After completion, create .planning/phases/04-submission-documentation/04-02-SUMMARY.md
</output>