---
phase: 04-submission-documentation
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - SUBMISSION.md
autonomous: true
requirements:
  - SUB-03
  - SUB-01
  - ID-01
  - ID-02
  - SET-03
  - DOC-01
  - DOC-03
  - REP-01
  - REP-02
must_haves:
  truths:
    - SUBMISSION checklist is complete, reproducible, and tied to concrete evidence for each claim.
    - Each critical requirement claim references deterministic command output or source artifact.
    - Final submission status can be evaluated without interpretation drift or hidden assumptions.
  artifacts:
    - path: SUBMISSION.md
      provides: authoritative evidence checklist and final readiness gate
  key_links:
    - from: SUBMISSION.md
      to: README.md
      via: narrative claims map to evidence checklist entries
      pattern: Track 2|Polkadot Hub Testnet|420420417
    - from: SUBMISSION.md
      to: docs/precompile-integration.md
      via: runtime integration claims point to detailed proof
      pattern: precompile|identityPrecompile.staticcall|IIdentityPrecompile.identity.selector
    - from: SUBMISSION.md
      to: docs/demo-script.md
      via: demo checklist rows link to deterministic walkthrough/fallback
      pattern: npm run testnet:config-check|split\(1 PAS\)
---

<objective>
Complete SUBMISSION.md as the final evidence-backed checklist with concrete artifact and command references.

Purpose: Satisfy SUB-03 by making final judging claims deterministic, replayable, and non-speculative.
Output: Submission checklist with requirement mapping, evidence rows, and deterministic validation gate commands.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-submission-documentation/04-RESEARCH.md
@.planning/phases/04-submission-documentation/04-VALIDATION.md
@.planning/phases/04-submission-documentation/04-01-PLAN.md
@.planning/phases/04-submission-documentation/04-02-PLAN.md
@README.md
@docs/precompile-integration.md
@docs/demo-script.md
@docs/judge-qa.md
@SUBMISSION.md
</context>

<scope_boundary>
Implement only SUBMISSION.md checklist completion with concrete evidence references for plan 04-03. Do not edit README.md or docs/*.md files in this plan.
</scope_boundary>

<tasks>

<task type="auto">
  <name>Task 1: Build requirement-to-evidence matrix rows for final submission checklist</name>
  <read_first>
    - SUBMISSION.md
    - .planning/REQUIREMENTS.md
    - README.md
    - docs/precompile-integration.md
    - docs/judge-qa.md
  </read_first>
  <files>SUBMISSION.md</files>
  <action>
    Restructure checklist content so each key claim includes requirement IDs, artifact path, deterministic PowerShell/Node command, expected output anchor, and status. Ensure minimum coverage includes SUB-03 plus linked proof IDs ID-01, ID-02, SET-03, DOC-01, DOC-03, REP-01, and REP-02 as applicable.

    Anti-hallucination rules for this task:
    - Do not mark evidence rows as passed unless command/output anchors exist in repository truth.
    - Do not invent tx hashes, explorer events, or route outputs.
    - If evidence is pending, keep status as NOT VERIFIED with clear next command.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $c = Get-Content SUBMISSION.md -Raw; foreach($req in @('SUB-03','ID-01','ID-02','SET-03','DOC-01','DOC-03','REP-01','REP-02')) { if ($c -notmatch [regex]::Escape($req)) { throw "SUBMISSION missing requirement mapping: $req" } }; foreach($field in @('Requirement IDs','Artifact','Command','Expected Output','Status')) { if ($c -notmatch [regex]::Escape($field)) { throw "SUBMISSION missing evidence field: $field" } }</automated>
  </verify>
  <acceptance_criteria>
    - SUBMISSION includes requirement-to-evidence mapping rows for mandatory IDs.
    - Each row contains reproducible command and expected output anchors.
  </acceptance_criteria>
  <done>SUBMISSION checklist schema is complete and enforces deterministic evidence for final review.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize concrete evidence references and cross-document value consistency</name>
  <read_first>
    - SUBMISSION.md
    - README.md
    - docs/precompile-integration.md
    - docs/demo-script.md
    - docs/judge-qa.md
  </read_first>
  <files>SUBMISSION.md</files>
  <action>
    Update checklist evidence references so chain ID, contract addresses, precompile address, and split proof references are exact matches across all judge-facing docs. Include explicit command snippets and expected key output lines used at release gate.

    Anti-hallucination rules for this task:
    - All values must match source docs exactly; no rounded, shortened, or inferred substitutes.
    - Keep evidence wording objective and replayable.
    - Remove any claim that cannot be validated through listed commands.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $files = @('README.md','SUBMISSION.md','docs/precompile-integration.md','docs/demo-script.md','docs/judge-qa.md'); $chain = '420420417'; foreach($f in $files){ if (-not (Select-String -Path $f -Pattern $chain -SimpleMatch)) { throw "Missing chain id in $f" } }; foreach($addr in @('0x233FE6112E5Ad4Db1c83358B30D581F837314BB1','0x1cf190eabe490B50AaBE91b4567ebe88126e8D24','0x0000000000000000000000000000000000000818')) { $hits = Select-String -Path $files -Pattern $addr -SimpleMatch; if ($hits.Count -lt 3) { throw "Insufficient cross-doc mentions for $addr" } }; $submission = Get-Content SUBMISSION.md -Raw; foreach($required in @('npm run lint','npm run build','npx hardhat test','npm run testnet:config-check','Config check passed','split(1 PAS)')) { if ($submission -notmatch [regex]::Escape($required)) { throw "SUBMISSION missing command/output anchor: $required" } }</automated>
  </verify>
  <acceptance_criteria>
    - Core evidence values are consistent across all judge-facing docs.
    - SUBMISSION command/output anchors are concrete and deterministic.
  </acceptance_criteria>
  <done>SUBMISSION concrete evidence references are complete and cross-doc consistent.</done>
</task>

<task type="auto">
  <name>Task 3: Run final deterministic docs gate and mark submission readiness status</name>
  <read_first>
    - SUBMISSION.md
    - package.json
    - scripts/testnet-config-check.js
  </read_first>
  <files>SUBMISSION.md</files>
  <action>
    Execute final docs gate command set and update final status wording in SUBMISSION.md to reflect only command-verified facts. Keep checklist output anchored to deterministic command outcomes and include explicit failure handling language for missing evidence.

    Anti-hallucination rules for this task:
    - Final status may only claim READY if all listed deterministic checks pass.
    - If any check cannot be run, status must explicitly record pending verification.
    - Never report green status from prior memory without rerunning listed commands.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $final = Get-Content SUBMISSION.md -Raw; if ($final -notmatch 'DOCUMATE SUBMISSION READY|NOT VERIFIED|PENDING') { throw 'SUBMISSION final status language missing required deterministic states' }; foreach($must in @('Requirement IDs','Artifact','Command','Expected Output','Status')) { if ($final -notmatch [regex]::Escape($must)) { throw "SUBMISSION missing docs gate field: $must" } }; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Final checklist status language reflects deterministic gate outcomes only.
    - SUBMISSION has no unresolved placeholder claims without explicit pending marker.
  </acceptance_criteria>
  <done>SUB-03 final submission gate is complete and evidence-locked for release review.</done>
</task>

</tasks>

<verification>
1. $ErrorActionPreference = 'Stop'; npm run lint
2. npm run build
3. npx hardhat test
4. npm run testnet:config-check
5. $files = @('README.md','SUBMISSION.md','docs/precompile-integration.md','docs/demo-script.md','docs/judge-qa.md'); foreach($f in $files){ if (-not (Test-Path $f)) { throw "Missing required file: $f" } }
6. $submission = Get-Content SUBMISSION.md -Raw; foreach($must in @('SUB-03','ID-01','ID-02','SET-03','DOC-01','DOC-03','REP-01','REP-02','Requirement IDs','Artifact','Command','Expected Output','Status')) { if ($submission -notmatch [regex]::Escape($must)) { throw "SUBMISSION missing: $must" } }
</verification>

<success_criteria>
- SUBMISSION.md satisfies SUB-03 with complete evidence-backed checklist rows.
- Required linked proof IDs (ID-01, ID-02, SET-03, DOC-01, DOC-03, REP-01, REP-02) are explicitly represented with deterministic evidence references.
- Scope boundary respected: only SUBMISSION.md changes are required in this plan.
</success_criteria>

<output>
After completion, create .planning/phases/04-submission-documentation/04-03-SUMMARY.md
</output>