---
phase: 03-end-to-end-prototype-test-on-testnet
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/app/dashboard/documents/[id]/page.tsx
  - src/app/api/documents/route.ts
  - src/app/api/documents/[id]/route.ts
  - src/app/api/activity/route.ts
  - src/app/api/validate-document/route.ts
  - src/components/document/ValidationTierBadge.tsx
autonomous: true
requirements:
  - DOC-01
  - DOC-03
must_haves:
  truths:
    - Anchored document proof is represented only with strict on-chain hash semantics in API and user flow.
    - TEE Gold/Silver/Bronze output is visible and interpretable in user-facing trust screens.
    - Activity and document responses provide evidence fields needed for judge-verifiable anchor and TEE claims.
  artifacts:
    - path: src/app/api/documents/route.ts
      provides: normalized anchor proof payload with strict transaction hash expectations
    - path: src/app/api/validate-document/route.ts
      provides: deterministic TEE classification contract fields
    - path: src/components/document/ValidationTierBadge.tsx
      provides: shared tier rendering evidence component
  key_links:
    - from: src/app/dashboard/documents/[id]/page.tsx
      to: src/app/api/documents/[id]/route.ts
      via: document proof rendering consumes strict anchor fields
      pattern: anchorStatus|transactionHash|anchorError
    - from: src/app/api/validate-document/route.ts
      to: src/components/document/ValidationTierBadge.tsx
      via: tier payload drives visible Gold/Silver/Bronze evidence rendering
      pattern: tier|tierName|attestation|confidence
---

<objective>
Validate on-chain hash anchoring and TEE trust-tier evidence end-to-end, including API contracts and user-visible rendering.

Purpose: Close DOC-01 and DOC-03 evidence requirements in a deterministic testnet run.
Output: Verifiable anchor and TEE evidence path from API producer to dashboard rendering.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-end-to-end-prototype-test-on-testnet/03-RESEARCH.md
@.planning/phases/03-end-to-end-prototype-test-on-testnet/03-VALIDATION.md
@.planning/phases/02-wire-gaps-found-in-audit/02-01-SUMMARY.md
@src/app/api/documents/route.ts
@src/app/api/validate-document/route.ts
</context>

<scope_boundary>
Implement only anchor hash + TEE tier + UI rendering evidence checks for plan 03-02. Do not include wallet-gate work from 03-01 or settlement/reputation work from 03-03.
</scope_boundary>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate strict anchor hash evidence contract across document and activity APIs</name>
  <read_first>
    - src/app/api/documents/route.ts
    - src/app/api/documents/[id]/route.ts
    - src/app/api/activity/route.ts
  </read_first>
  <files>src/app/api/documents/route.ts, src/app/api/documents/[id]/route.ts, src/app/api/activity/route.ts</files>
  <behavior>
    - Test 1: Anchored records expose only strict `0x` + 64-hex transaction hashes as valid chain proof.
    - Test 2: Activity endpoint projects DOCUMENT_ANCHOR evidence only for valid strict hashes and non-failed anchor state.
  </behavior>
  <action>
    Preserve and harden strict anchor normalization and filtering contract so anchored-state evidence remains deterministic across list, by-id, and activity APIs. Ensure the fields required for trust decisions (`transactionHash`, `anchorStatus`, `anchorError`) remain consistent and fail-closed.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }; $Wallet = "0x1111111111111111111111111111111111111111"; Select-String -Path 'src/app/dashboard/documents/[id]/page.tsx','src/app/api/**/*.ts' -Pattern 'offchain-anchor:' -SimpleMatch | Out-Null; $docs = Invoke-RestMethod "http://localhost:3000/api/documents?walletAddress=$Wallet"; $activity = Invoke-RestMethod "http://localhost:3000/api/activity?walletAddress=$Wallet"; $docs | ConvertTo-Json -Depth 10 | Out-Null; $activity | ConvertTo-Json -Depth 10 | Out-Null; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Anchor evidence path remains strict-hash-only and fail-closed.
    - API contracts expose deterministic anchor metadata fields for verification.
  </acceptance_criteria>
  <done>DOC-01 evidence path is deterministic and compatible with testnet verification scripts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate TEE classification payload contract and renderability in trust UX</name>
  <read_first>
    - src/app/api/validate-document/route.ts
    - src/components/document/ValidationTierBadge.tsx
    - src/app/dashboard/documents/new/page.tsx
  </read_first>
  <files>src/app/api/validate-document/route.ts, src/components/document/ValidationTierBadge.tsx, src/app/dashboard/documents/new/page.tsx</files>
  <behavior>
    - Test 1: TEE validation response includes deterministic keys for tier, tierName, attestation, confidence, and worker evidence.
    - Test 2: UI trust flow visibly renders Gold/Silver/Bronze state from producer payload.
  </behavior>
  <action>
    Ensure validate-document route emits stable TEE evidence contract fields and the trust UI consumes these fields without fallback ambiguity. Keep tier rendering explicit via shared tier component so execution evidence can directly tie API payload to visual trust state.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }; $form = @{ file = Get-Item ".\docs\audit\01-01-document-anchor-evidence.md" }; $tee = Invoke-RestMethod -Method Post -Uri "http://localhost:3000/api/validate-document" -Form $form; if ($tee.success -ne $true) { throw "TEE validation did not return success=true" }; foreach ($k in @('tier','tierName','attestation','confidence','phalaWorker')) { if (-not $tee.PSObject.Properties.Name.Contains($k)) { throw "Missing TEE key: $k" } }; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - TEE response contract includes required trust-tier evidence keys.
    - Gold/Silver/Bronze state is visibly renderable from deterministic payload fields.
  </acceptance_criteria>
  <done>DOC-03 producer-to-consumer evidence contract is verifiable and UI-visible.</done>
</task>

<task type="auto">
  <name>Task 3: Produce deterministic 03-02 evidence command sequence for anchor and TEE checks</name>
  <read_first>
    - .planning/phases/03-end-to-end-prototype-test-on-testnet/03-VALIDATION.md
    - docs/audit/01-01-document-anchor-evidence.md
    - src/app/api/validate-document/route.ts
  </read_first>
  <files>.planning/phases/03-end-to-end-prototype-test-on-testnet/03-VALIDATION.md</files>
  <action>
    Align 03-02 verification steps to deterministic PowerShell sequence covering strict anchor payload probes and TEE fixture upload. Keep fixed wallet address usage for all wallet-address API checks and ensure command outputs are capture-ready for SUMMARY evidence.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }; $Wallet = "0x1111111111111111111111111111111111111111"; Invoke-RestMethod "http://localhost:3000/api/documents?walletAddress=$Wallet" | ConvertTo-Json -Depth 10 | Out-Null; Invoke-RestMethod "http://localhost:3000/api/activity?walletAddress=$Wallet" | ConvertTo-Json -Depth 10 | Out-Null; $form = @{ file = Get-Item ".\docs\audit\01-01-document-anchor-evidence.md" }; Invoke-RestMethod -Method Post -Uri "http://localhost:3000/api/validate-document" -Form $form | ConvertTo-Json -Depth 8 | Out-Null</automated>
  </verify>
  <acceptance_criteria>
    - 03-02 command sequence is deterministic on Windows PowerShell.
    - All wallet APIs in verification use fixed wallet input.
  </acceptance_criteria>
  <done>03-02 can be executed and evidenced without ad-hoc command interpretation.</done>
</task>

</tasks>

<verification>
1. $ErrorActionPreference = 'Stop'; $Wallet = "0x1111111111111111111111111111111111111111"
2. $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }
3. Select-String -Path 'src/app/dashboard/documents/[id]/page.tsx','src/app/api/**/*.ts' -Pattern 'offchain-anchor:' -SimpleMatch
4. Invoke-RestMethod "http://localhost:3000/api/documents?walletAddress=$Wallet" | ConvertTo-Json -Depth 10
5. Invoke-RestMethod "http://localhost:3000/api/activity?walletAddress=$Wallet" | ConvertTo-Json -Depth 10
6. $form = @{ file = Get-Item ".\docs\audit\01-01-document-anchor-evidence.md" }; Invoke-RestMethod -Method Post -Uri "http://localhost:3000/api/validate-document" -Form $form | ConvertTo-Json -Depth 10
7. npm run lint
8. npm run build
</verification>

<success_criteria>
- DOC-01 anchor evidence is strict, deterministic, and visible in API response contracts.
- DOC-03 TEE tier evidence is deterministic in API payload and visible in trust-facing UI flow.
- Scope boundary respected: no settlement split or reputation provenance implementation appears in this plan.
</success_criteria>

<output>
After completion, create .planning/phases/03-end-to-end-prototype-test-on-testnet/03-02-SUMMARY.md
</output>
