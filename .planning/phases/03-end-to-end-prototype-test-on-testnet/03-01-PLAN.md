---
phase: 03-end-to-end-prototype-test-on-testnet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useEVMWallet.ts
  - src/hooks/useDocuMateContract.ts
  - src/app/dashboard/documents/new/page.tsx
  - src/app/api/documents/route.ts
  - src/app/api/activity/route.ts
autonomous: true
requirements:
  - ID-01
  - ID-02
must_haves:
  truths:
    - Wallet connection in the prototype uses Polkadot Hub testnet and enforces the configured identity runtime path.
    - Draft and critical-action flows are blocked when verification gate is not satisfied.
    - Draft flow evidence can be retrieved for a fixed wallet from deterministic API envelopes.
  artifacts:
    - path: src/hooks/useEVMWallet.ts
      provides: deterministic wallet connect and chain enforcement hooks for the testnet flow
    - path: src/hooks/useDocuMateContract.ts
      provides: verification gate check for critical actions
    - path: src/app/api/documents/route.ts
      provides: wallet-scoped document draft evidence envelope
  key_links:
    - from: src/hooks/useEVMWallet.ts
      to: src/hooks/useDocuMateContract.ts
      via: connected signer address used by verification gate check
      pattern: connectMetaMask|checkVerified|isVerified
    - from: src/app/dashboard/documents/new/page.tsx
      to: src/app/api/documents/route.ts
      via: draft creation and retrieval evidence contract
      pattern: /api/documents
---

<objective>
Validate wallet connect through KILT verification gate and prove DocuWriter draft evidence is retrievable with deterministic inputs.

Purpose: Lock ID-01 and ID-02 behaviors into reproducible evidence before anchor, settlement, and reputation validations.
Output: Wallet-and-verify-gated draft flow with deterministic PowerShell verification evidence for fixed wallet address.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-end-to-end-prototype-test-on-testnet/03-RESEARCH.md
@.planning/phases/03-end-to-end-prototype-test-on-testnet/03-VALIDATION.md
@.planning/phases/02-wire-gaps-found-in-audit/02-01-SUMMARY.md
@src/hooks/useEVMWallet.ts
@src/hooks/useDocuMateContract.ts
</context>

<scope_boundary>
Implement only wallet connect, KILT verify gate checks, and draft flow evidence contracts for plan 03-01. Do not include anchoring, TEE tier rendering, settlement split verification, or reputation provenance work in this plan.
</scope_boundary>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce deterministic wallet connect and verification-gate contract for critical actions</name>
  <read_first>
    - src/hooks/useEVMWallet.ts
    - src/hooks/useDocuMateContract.ts
    - scripts/testnet-config-check.js
  </read_first>
  <files>src/hooks/useEVMWallet.ts, src/hooks/useDocuMateContract.ts</files>
  <behavior>
    - Test 1: Wallet connect path enforces Polkadot Hub EVM chain requirement and fails clearly on mismatch.
    - Test 2: checkVerified/isVerified gate remains mandatory for marketplace/document critical actions.
  </behavior>
  <action>
    Confirm and harden the wallet connection contract so the connected account is validated against the configured testnet chain and passed into verification-gated contract interactions. Keep the KILT verification gate fail-closed for critical actions by routing through existing checkVerified/isVerified logic with explicit actionable errors.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; node scripts/testnet-config-check.js; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Critical contract-action path cannot proceed without passing verification gate.
    - Verification gate behavior is deterministic and testnet-config aligned.
  </acceptance_criteria>
  <done>ID-01 and ID-02 enforcement is stable at hook layer for wallet-connected actions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire and prove DocuWriter draft flow evidence envelope for fixed wallet input</name>
  <read_first>
    - src/app/dashboard/documents/new/page.tsx
    - src/app/api/documents/route.ts
    - src/app/api/activity/route.ts
  </read_first>
  <files>src/app/dashboard/documents/new/page.tsx, src/app/api/documents/route.ts, src/app/api/activity/route.ts</files>
  <action>
    Ensure draft creation and retrieval flow exposes deterministic evidence from API envelopes needed for downstream phase checks. Keep response shape stable and wallet-scoped so fixed test wallet probes can verify draft presence and activity envelope integrity without UI-only assumptions.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }; $Wallet = "0x1111111111111111111111111111111111111111"; $docs = Invoke-RestMethod "http://localhost:3000/api/documents?walletAddress=$Wallet"; $activity = Invoke-RestMethod "http://localhost:3000/api/activity?walletAddress=$Wallet"; if ($null -eq $docs.success) { throw "Documents envelope missing success" }; if ($null -eq $activity.success) { throw "Activity envelope missing success" }; $docs | ConvertTo-Json -Depth 10 | Out-Null; $activity | ConvertTo-Json -Depth 10 | Out-Null; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Fixed-wallet documents API response is valid and deterministic in envelope shape.
    - Fixed-wallet activity API response is valid and deterministic in envelope shape.
  </acceptance_criteria>
  <done>Draft flow produces reproducible evidence payloads for testnet user journey checkpoints.</done>
</task>

<task type="auto">
  <name>Task 3: Capture execution-ready verification evidence script path for 03-01 gate</name>
  <read_first>
    - .planning/phases/03-end-to-end-prototype-test-on-testnet/03-VALIDATION.md
    - .planning/phases/03-end-to-end-prototype-test-on-testnet/03-RESEARCH.md
    - docs/audit/01-01-document-anchor-evidence.md
  </read_first>
  <files>.planning/phases/03-end-to-end-prototype-test-on-testnet/03-VALIDATION.md</files>
  <action>
    Align 03-01 execution verification notes with deterministic PowerShell command order for wallet connect gate and draft evidence probes. Keep commands reproducible on Windows and explicitly fixed to wallet `0x1111111111111111111111111111111111111111` for API checks.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }; $Wallet = "0x1111111111111111111111111111111111111111"; Invoke-RestMethod "http://localhost:3000/api/documents?walletAddress=$Wallet" | ConvertTo-Json -Depth 8 | Out-Null; Invoke-RestMethod "http://localhost:3000/api/activity?walletAddress=$Wallet" | ConvertTo-Json -Depth 8 | Out-Null</automated>
  </verify>
  <acceptance_criteria>
    - 03-01 verification order is explicit and deterministic for PowerShell execution.
    - Wallet-address-dependent API checks all use the fixed wallet input.
  </acceptance_criteria>
  <done>03-01 execution gate has reproducible command-level evidence instructions.</done>
</task>

</tasks>

<verification>
1. $ErrorActionPreference = 'Stop'; node scripts/testnet-config-check.js
2. npm run lint
3. $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }
4. $Wallet = "0x1111111111111111111111111111111111111111"; Invoke-RestMethod "http://localhost:3000/api/documents?walletAddress=$Wallet" | ConvertTo-Json -Depth 10
5. $Wallet = "0x1111111111111111111111111111111111111111"; Invoke-RestMethod "http://localhost:3000/api/activity?walletAddress=$Wallet" | ConvertTo-Json -Depth 10
</verification>

<success_criteria>
- ID-01 and ID-02 behaviors are demonstrably enforced in wallet-connect and critical-action gate paths.
- Draft flow evidence for fixed wallet can be retrieved from deterministic API envelopes.
- Scope boundary respected: no anchor/TEE/split/reputation implementation work appears in this plan.
</success_criteria>

<output>
After completion, create .planning/phases/03-end-to-end-prototype-test-on-testnet/03-01-SUMMARY.md
</output>
