---
phase: 03-end-to-end-prototype-test-on-testnet
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/app/api/market/purchase/route.ts
  - src/lib/contracts/marketplace.ts
  - src/app/api/activity/route.ts
  - src/app/api/directory/profile/route.ts
  - src/lib/reputation/tagging.ts
  - src/app/dashboard/profile/page.tsx
autonomous: true
requirements:
  - SET-03
  - REP-01
must_haves:
  truths:
    - Marketplace purchase evidence demonstrates deterministic 75/20/5 split across contract and API surfaces.
    - Purchase trust signals in activity/profile views are tied to on-chain transaction evidence.
    - Reputation tags shown as trust evidence include provenance pointers and are not tag-only assertions.
  artifacts:
    - path: src/app/api/market/purchase/route.ts
      provides: persisted split payload and purchase evidence envelope
    - path: src/app/api/directory/profile/route.ts
      provides: profile trust payload containing provenance-ready tag evidence
    - path: src/lib/reputation/tagging.ts
      provides: reputation provenance derivation contract
  key_links:
    - from: src/lib/contracts/marketplace.ts
      to: src/app/api/market/purchase/route.ts
      via: calculateSplit and purchase transaction parity with API split output
      pattern: calculateSplit|split|txHash
    - from: src/lib/reputation/tagging.ts
      to: src/app/dashboard/profile/page.tsx
      via: source-and-pointer provenance rendered in live trust UI
      pattern: source|txHash|pocHash|breachId|issuedAt
---

<objective>
Validate marketplace purchase execution, deterministic 75/20/5 split checks, and reputation provenance evidence in live user-facing outputs.

Purpose: Close SET-03 and REP-01 with chain-linked proof artifacts required for final hackathon credibility.
Output: Reproducible purchase-split evidence and provenance-backed reputation trust payloads for fixed-wallet verification.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-end-to-end-prototype-test-on-testnet/03-RESEARCH.md
@.planning/phases/03-end-to-end-prototype-test-on-testnet/03-VALIDATION.md
@.planning/phases/02-wire-gaps-found-in-audit/02-01-SUMMARY.md
@src/app/api/market/purchase/route.ts
@src/app/api/directory/profile/route.ts
@src/lib/reputation/tagging.ts
</context>

<scope_boundary>
Implement only marketplace purchase verification, 75/20/5 split parity checks, and reputation provenance evidence for plan 03-03. Do not add wallet-connect/KILT gate work or anchor/TEE implementation from prior plans.
</scope_boundary>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate deterministic 75/20/5 settlement parity between contract and purchase API payload</name>
  <read_first>
    - src/lib/contracts/marketplace.ts
    - src/app/api/market/purchase/route.ts
    - scripts/testnet-config-check.js
  </read_first>
  <files>src/lib/contracts/marketplace.ts, src/app/api/market/purchase/route.ts</files>
  <behavior>
    - Test 1: Contract split math yields deterministic 75/20/5 partition for fixed test price inputs.
    - Test 2: Purchase API split payload matches contract-derived values for creator/company/staking amounts.
  </behavior>
  <action>
    Keep split enforcement logic deterministic and aligned across contract helper and purchase API write path. Ensure API response payload explicitly includes creator/company/staking split values derived from canonical 75/20/5 constants and tied to a strict tx hash proof.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; npm run testnet:config-check; npm run contracts:test; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Split outputs are deterministic and mathematically consistent with 75/20/5 requirement.
    - Purchase payload includes split values and strict tx hash evidence fields.
  </acceptance_criteria>
  <done>SET-03 is reproducibly evidenced at both contract and API layers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate marketplace purchase and activity evidence retrieval for fixed wallet input</name>
  <read_first>
    - src/app/api/activity/route.ts
    - src/app/api/market/purchase/route.ts
    - src/app/dashboard/marketplace/page.tsx
  </read_first>
  <files>src/app/api/activity/route.ts, src/app/api/market/purchase/route.ts</files>
  <behavior>
    - Test 1: Post-purchase activity envelope includes transaction-linked purchase evidence for fixed wallet.
    - Test 2: Activity retrieval preserves deterministic envelope keys used for final summary evidence.
  </behavior>
  <action>
    Ensure activity projection and purchase API contracts expose stable fields needed to prove purchase completion and split evidence linkage by tx hash. Keep wallet-scoped retrieval deterministic for fixed-wallet PowerShell probes.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }; $Wallet = "0x1111111111111111111111111111111111111111"; $activity = Invoke-RestMethod "http://localhost:3000/api/activity?walletAddress=$Wallet"; if ($null -eq $activity.success) { throw "Activity envelope missing success" }; $activity | ConvertTo-Json -Depth 10 | Out-Null; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Wallet-scoped activity evidence can be retrieved deterministically for fixed wallet.
    - Purchase evidence fields are stable enough for summary and judge-proof capture.
  </acceptance_criteria>
  <done>Purchase-event evidence retrieval contract is deterministic and execution-ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Validate reputation provenance evidence contract in profile trust outputs</name>
  <read_first>
    - src/lib/reputation/tagging.ts
    - src/app/api/directory/profile/route.ts
    - src/app/dashboard/profile/page.tsx
  </read_first>
  <files>src/lib/reputation/tagging.ts, src/app/api/directory/profile/route.ts, src/app/dashboard/profile/page.tsx</files>
  <behavior>
    - Test 1: Reputation trust payload includes provenance source and at least one pointer field (`txHash`, `pocHash`, or `breachId`) for evidence-bearing tags.
    - Test 2: Profile trust UI consumes provenance fields without reducing trust claims to tag strings only.
  </behavior>
  <action>
    Enforce and render provenance-ready reputation contract with explicit source and evidence-pointer fields (`txHash`, `pocHash`, `breachId`, `issuedAt`) in profile trust outputs. Keep behavior aligned to REP-01 expectation that displayed trust tags are backed by on-chain or evidence-linked provenance.
  </action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }; $Wallet = "0x1111111111111111111111111111111111111111"; $profile = Invoke-RestMethod "http://localhost:3000/api/directory/profile?walletAddress=$Wallet"; if ($null -eq $profile.success) { throw "Profile envelope missing success" }; $json = $profile | ConvertTo-Json -Depth 12; if ($json -notmatch 'source') { throw "Missing provenance source in profile payload" }; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Profile trust payload includes provenance semantics, not tag-only evidence.
    - Reputation trust rendering can point to concrete evidence pointers for displayed claims.
  </acceptance_criteria>
  <done>REP-01 evidence contract is validated for live trust output consumption.</done>
</task>

</tasks>

<verification>
1. $ErrorActionPreference = 'Stop'; npm run testnet:config-check
2. npm run contracts:test
3. $serverStatus = (Invoke-WebRequest -UseBasicParsing "http://localhost:3000").StatusCode; if ($serverStatus -ne 200) { throw "Precondition failed: local server must be running at http://localhost:3000" }
4. $Wallet = "0x1111111111111111111111111111111111111111"; Invoke-RestMethod "http://localhost:3000/api/activity?walletAddress=$Wallet" | ConvertTo-Json -Depth 10
5. $Wallet = "0x1111111111111111111111111111111111111111"; Invoke-RestMethod "http://localhost:3000/api/directory/profile?walletAddress=$Wallet" | ConvertTo-Json -Depth 12
6. npm run lint
7. npm run build
</verification>

<success_criteria>
- SET-03 evidence proves deterministic 75/20/5 split parity between contract checks and API payloads.
- REP-01 evidence proves live trust outputs include provenance source and evidence pointers.
- Scope boundary respected: no wallet-gate or anchor/TEE implementation appears in this plan.
</success_criteria>

<output>
After completion, create .planning/phases/03-end-to-end-prototype-test-on-testnet/03-03-SUMMARY.md
</output>
