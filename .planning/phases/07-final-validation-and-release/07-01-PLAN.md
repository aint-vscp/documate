---
phase: 07-final-validation-and-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-final-validation-and-release/
autonomous: true
requirements:
  - SUB-03
must_haves:
  truths:
    - "Release cannot proceed unless all mandatory quality gates pass."
    - "Any failed gate blocks tagging."
  artifacts:
    - path: .planning/phases/07-final-validation-and-release/
      provides: "Release gate execution evidence"
---

<objective>
Run final automated release gates for application quality, security posture, contracts, and testnet consistency.

Output: deterministic gate-pass evidence for release readiness.
</objective>

<context>
@.planning/phases/07-final-validation-and-release/07-RESEARCH.md
@.planning/phases/07-final-validation-and-release/07-VALIDATION.md
@package.json
@hardhat.config.js
@scripts/testnet-config-check.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run vulnerability gate</name>
  <read_first>
    - package.json
  </read_first>
  <files>
    - package.json
  </files>
  <action>
    Execute npm audit at high severity threshold and record findings.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; npm audit --audit-level=high | Out-Null; Write-Host 'audit-gate-ok'"</automated>
  </verify>
  <acceptance_criteria>
    No blocking high-or-higher vulnerabilities remain unresolved for release.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run static and app build gates</name>
  <read_first>
    - package.json
  </read_first>
  <files>
    - src/
  </files>
  <action>
    Execute lint and production build to verify code health and app buildability.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; npm run lint | Out-Null; npm run build | Out-Null; Write-Host 'lint-build-gates-ok'"</automated>
  </verify>
  <acceptance_criteria>
    Lint and build succeed with no release-blocking errors.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run contract and testnet consistency gates</name>
  <read_first>
    - hardhat.config.js
    - scripts/testnet-config-check.js
  </read_first>
  <files>
    - contracts/
    - scripts/
  </files>
  <action>
    Execute contract tests and testnet config check to prove release evidence remains valid.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; npm run contracts:test | Out-Null; npm run testnet:config-check | Out-Null; Write-Host 'contract-testnet-gates-ok'"</automated>
  </verify>
  <acceptance_criteria>
    Contract suite and testnet config check both pass.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- powershell -NoProfile -Command "$ErrorActionPreference='Stop'; npm run lint | Out-Null; Write-Host 'phase-07-01-lint-gate-ok'"
</verification>

<success_criteria>
- All release gates for security/build/contracts/testnet are green.
- Evidence is ready for release checklist verification.
</success_criteria>

<output>
After completion, create .planning/phases/07-final-validation-and-release/07-01-SUMMARY.md.
</output>
