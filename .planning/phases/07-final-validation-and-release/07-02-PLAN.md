---
phase: 07-final-validation-and-release
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - README.md
  - SUBMISSION.md
  - docs/
autonomous: true
requirements:
  - SUB-03
must_haves:
  truths:
    - "Submission checklist claims must map to existing evidence artifacts."
    - "Critical constants must remain consistent across all judge-facing docs."
  artifacts:
    - path: SUBMISSION.md
      provides: "Release readiness checklist and evidence links"
    - path: docs/
      provides: "Proof documents and demo artifact"
---

<objective>
Verify final submission evidence coherence across docs and artifact paths before release tagging.

Output: validated checklist confidence for release handoff.
</objective>

<context>
@.planning/phases/07-final-validation-and-release/07-RESEARCH.md
@.planning/phases/07-final-validation-and-release/07-VALIDATION.md
@README.md
@SUBMISSION.md
@docs/precompile-integration.md
@docs/demo-script.md
@docs/judge-qa.md
@docs/demo-video.mp4
@package.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify all required submission artifacts exist</name>
  <read_first>
    - README.md
    - SUBMISSION.md
  </read_first>
  <files>
    - README.md
    - SUBMISSION.md
    - docs/
  </files>
  <action>
    Assert existence of all required docs and demo artifact expected by release checklist.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; $required = @('README.md','SUBMISSION.md','docs/precompile-integration.md','docs/demo-script.md','docs/judge-qa.md','docs/demo-video.mp4'); foreach($f in $required){ if (!(Test-Path $f)) { throw \"missing-artifact: $f\" } }; Write-Host 'artifact-existence-ok'"</automated>
  </verify>
  <acceptance_criteria>
    All required submission artifacts exist at expected paths.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify critical constants are cross-doc consistent</name>
  <read_first>
    - README.md
    - SUBMISSION.md
    - docs/precompile-integration.md
    - docs/demo-script.md
    - docs/judge-qa.md
  </read_first>
  <files>
    - README.md
    - SUBMISSION.md
    - docs/
  </files>
  <action>
    Verify chain ID, contract addresses, precompile address, and split value are present and coherent in judge-facing documents.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; $files = @('README.md','SUBMISSION.md','docs/precompile-integration.md','docs/demo-script.md','docs/judge-qa.md'); foreach($v in @('420420417','0x233FE6112E5Ad4Db1c83358B30D581F837314BB1','0x1cf190eabe490B50AaBE91b4567ebe88126e8D24','0x0000000000000000000000000000000000000818','75/20/5')) { if ((Select-String -Path $files -Pattern [regex]::Escape($v) -SimpleMatch).Count -lt 2) { throw \"insufficient-evidence: $v\" } }; Write-Host 'constants-coherence-ok'"</automated>
  </verify>
  <acceptance_criteria>
    Critical constants are sufficiently represented across release evidence docs.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify demo render command contract and checklist readiness</name>
  <read_first>
    - package.json
    - SUBMISSION.md
  </read_first>
  <files>
    - package.json
    - SUBMISSION.md
  </files>
  <action>
    Confirm package render command matches canonical output path and checklist text is compatible with current release evidence.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; (Get-Content package.json -Raw) | Select-String -Pattern '\"video:render\"\s*:\s*\"npx remotion render HackathonDemo90 docs/demo-video.mp4\"' | Out-Null; $submission = Get-Content SUBMISSION.md -Raw; if ($submission.Length -lt 100) { throw 'submission-checklist-too-short' }; Write-Host 'render-checklist-contract-ok'"</automated>
  </verify>
  <acceptance_criteria>
    Render command contract and checklist baseline are both valid.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- powershell -NoProfile -Command "$ErrorActionPreference='Stop'; npm run lint | Out-Null; Write-Host 'phase-07-02-lint-gate-ok'"
</verification>

<success_criteria>
- Submission evidence graph is coherent and complete.
- Checklist confidence is high enough to proceed to tagging.
</success_criteria>

<output>
After completion, create .planning/phases/07-final-validation-and-release/07-02-SUMMARY.md.
</output>
